/**
 * @file index.ts
 * @description Barrel for the shared domain core (`@core/domain`): entities,
 *   aggregates, value objects, domain events/services/errors, and repository
 *   ports. Populated incrementally by the @core migration roadmap
 *   (docs/architecture/CORE_MIGRATION_ROADMAP_ES.md); empty during P0 scaffold.
 * @layer domain
 */

export {};
