/**
 * @file index.ts
 * @description Barrel for the shared application core (`@core/application`):
 *   use cases, command/query handlers, and use-case-contract DTOs. Populated
 *   incrementally by the @core migration roadmap
 *   (docs/architecture/CORE_MIGRATION_ROADMAP_ES.md); empty during P0 scaffold.
 * @layer application
 */

export {};
