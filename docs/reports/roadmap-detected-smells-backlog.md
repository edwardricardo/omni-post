# Roadmap — Detected Smells Backlog

Living, trackable log of smells / anti-patterns / dead-code detected
**during** a roadmap task but **outside that task's scope**. Findings are
neither fixed inline (anti-churn) nor hidden (anti-time-bomb): they are
recorded here with a pending verdict and closed in a dedicated cleanup
sweep **after all phases are complete**.

Process:

- A finding outside the current task's scope → add an entry here, mention
  it, continue the task strictly scoped. New code still follows current
  canon so no new time bombs are introduced.
- After **Fase 0** closes: revisit the code from the Bloque B iterations
  and feed this backlog with findings.
- The **cleanup sweep** runs after **all phases** close, item-by-item,
  with the 3-question filter (origin + purpose + duplication) and an
  explicit verdict (FIX / DEFER / WONT_FIX).

Verdict legend: `PENDING` (not yet adjudicated) · `FIX` · `DEFER` ·
`WONT_FIX`.

| ID       | Origin                                                                      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Original purpose                                                                                                                                                                                                                                                                                                | Verdict                                 | Close phase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SMELL-50 | hoisting audit (raised during turbopack-root RCA, 2026-06-15)               | Monorepo-wide sweep of ALL hoistings. Hoisting is the blunt anti-pattern this codebase deliberately avoids — it breaks pnpm's isolated layout, enables phantom deps (importing a package NOT declared in your own package.json because it leaked into a shared/root node_modules), and was REJECTED as the Next `turbopack.root` fix (chose per-environment #2 over `public-hoist-pattern[]=next*`). TASK: audit every hoist across the repo — all `.npmrc` `public-hoist-pattern` / `hoist-pattern` / `node-linker=hoisted` / `shamefully-hoist` entries (root `.npmrc` today is only `public-hoist-pattern[]=@stryker-mutator/*` — confirm it is the ONLY one and still justified) AND every reliance on hoisted/phantom deps (any import of a package absent from the importing unit's package.json dependencies). Each hoist gets a verdict: justify with reason, or remove + declare the dep properly.                                                                                                                                                                                                                                                                                                                                                                                                                                           | hoisting crept in as a convenience (stryker) and is a recurring candidate shortcut for resolution fixes; needs a deliberate audit so the isolated-layout discipline holds repo-wide.                                                                                                                            | PENDING                                 | Dedicated hoisting sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SMELL-1  | F0-API-1 recon                                                              | Old integration tests use the skip-if-API-down anti-pattern (`apps/api/tests/integration/crisisRoutes.test.ts` et al.: `console.log("⚠️ … will be skipped")` + early return) instead of the fail-loud canon used by `sagaCustomerFlow.test.ts` (`assert.ok(apiAvailable, …)`). Effect: green CI that silently tested nothing — a latent time bomb.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Defensive test scaffolding from before the fail-loud canon was adopted.                                                                                                                                                                                                                                         | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-2  | F0-API-1 recon                                                              | `apps/api/src/custom-reports/customReportRoutes.ts:getAccountId()` casts `request as unknown as Record<string, unknown>` to read `request.user?.accountId` instead of the typed `request.customerUser?.accountId` exposed by `requireClientAuth`. Untyped accessor; inconsistent with the canonical customer-auth shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Legacy accessor predating the typed `CustomerRequestUser` augmentation.                                                                                                                                                                                                                                         | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-3  | F0-CLI-1 recon + AUDIT-1                                                    | Repurpose variant approve/reject UI was removed from `apps/client/app/dashboard/ai/repurpose/page.tsx`: it was wired to the generic `/api/backend/approvals/:id/:action` endpoint (not repurpose-specific) and assumed a nested `variants[]` shape the real DTO does not carry. The backend half is the matching forgotten-feature: `ApproveRepurposeVariantUseCase`/`RejectRepurposeVariantUseCase` are registered in DI (`apps/api/src/infrastructure/container/setupRepurposeUseCases.ts:54-72`) with their Prisma adapters (`PrismaApproveVariantAdapter`, `PrismaRejectVariantAdapter`) but no route consumes them. Closing this means a dedicated repurpose-approve/reject API task plus the matching UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Variant-management surface scaffolded ahead of its backend contract.                                                                                                                                                                                                                                            | PENDING                                 | Future repurpose-approve task / post-fases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SMELL-4  | F0-CLI-1 recon                                                              | AI sub-pages (`/dashboard/ai/optimizer`, `/ai/trends`, `/ai/repurpose`, `/ai/templates`, `/ai/analytics`) are not reachable from the dashboard sidebar — the nav has a single `AI` entry pointing only at `/dashboard/ai/generate` (`apps/client/app/dashboard/layout.tsx`). Global, pre-existing information-architecture gap, not repurpose-specific.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Sidebar predates the AI sub-page expansion.                                                                                                                                                                                                                                                                     | FIX                                     | F0-CLI-3 (2026-05-20) — sidebar AI collapsible sub-menu with 6 entries (Generate / Trends / Repurpose / Optimizer / Templates / AI Analytics); auto-expands on `/dashboard/ai/*` routes; aria-expanded toggle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| SMELL-5  | AUDIT-1 (B1, commit 65b84c7)                                                | `apps/api/src/ai/structuredSchemas.ts` repeats the pattern `jsonSchema: z.toJSONSchema(<schema>) as Record<string, unknown>` **four** times (lines 120, 129, 136, 149). The cast widens `z.toJSONSchema()`'s precise return type to an opaque `Record`. Either the consumer's `StructuredOutputSpec.jsonSchema` field type should narrow to match zod's actual return, or the cast site should narrow — currently the boundary is fuzzy in both directions and the fitness #3 grep does not catch it (it is not `as any`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Cross-provider structured-output glue between zod and SDK shapes built in B1.                                                                                                                                                                                                                                   | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-6  | AUDIT-1 (pre-existing, surfaced)                                            | `apps/api/src/infrastructure/container/types.ts:147,150,153,156` carries task-code references in comments (`P3-A — publish flow integration`, `P1-DI-7 — link tracking`, `P1-DI-8 — crisis mode`). Violates the comment-quality rule "no references to sprint numbers, implementation phases, or development timeline". Pre-B/F0 (genesis commit `^5603d6b`), spotted while F0 commits added new symbols to the same file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Provenance breadcrumbs from an earlier sprint-tracked refactor.                                                                                                                                                                                                                                                 | RESOLVED                                | RESOLVED (2026-05-29 backlog audit) — `grep "P3-A\|P1-DI" apps/api/src/infrastructure/container/types.ts` → 0 matches. Cleaned in an interim refactor without the verdict being updated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SMELL-7  | AUDIT-1 (pre-existing, surfaced)                                            | `apps/api/src/index.ts:883` uses a double cast `(app as unknown as Record<string, unknown>).sagaIntegration as ...` to read the saga decorator off the Fastify instance, instead of augmenting the `FastifyInstance` module to declare the property. Type smell of the worst class (bypasses two safety nets at once). Pre-B/F0 (commit `069155c5`), spotted while F0 wired the repurpose consumers into the same shutdown block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Quick saga-integration getter ahead of a proper Fastify decorator typing.                                                                                                                                                                                                                                       | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-8  | AUDIT-1 (pre-existing, surfaced)                                            | `apps/api/src/ai/orchestrator.ts:58,60` casts `(value as Record<string, unknown>)` inside `stableStringify` after the function has already narrowed `value` to `typeof === "object" && !== null && !Array.isArray`. The cast is replaceable with a proper type guard so TypeScript narrows without `as`. Pre-B/F0 (commit `b06a189e`), spotted while B1 edited the surrounding orchestrator surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Determinism helper for AI call caching, written ahead of strictest type rigor.                                                                                                                                                                                                                                  | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-9  | F0-WRK-2 recon                                                              | `apps/api/src/application/inbox/handlers/InboxEventHandlers.ts` is registered in DI (`setupInboxUseCases.ts:236-243`) but **never subscribed** to `EventDispatcher`. `onSocialMessageReceived` (creates inbox notification) and `onSocialMessageReplied` have **zero callers** in `apps/api/src`. The notification side of inbox event handling is dormant — domain event `SocialMessageReceived` is emitted by `SocialMessageAggregate` but the handler that should create notifications never runs. **Status uncertain**: dead code, forgotten-feature awaiting subscription wiring, or scheduled for a future task — needs classification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Notification routing on message arrival, scaffolded ahead of its subscription/dispatch wiring.                                                                                                                                                                                                                  | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-10 | F0-WRK-2 recon                                                              | Two parallel event-dispatch systems coexist in the repo: `EventDispatcher` (`apps/api/src/domain/events/DomainEvent.ts:67-78`, outbox-driven, `DomainEventHandler<T>` interface, used by `IntegrationEventDeliveryHandler`) and `EventService`/`sagaEventService` (`apps/api/src/events/EventService.ts`, Redis pub/sub, `EventHandler` from `@shared/events`, used for `EVENT_TYPES.POST_PUBLISHED`/`USER_ACTION`/`SYSTEM_HEALTH`). The two dispatch different event catalogs with different handler contracts. **Status uncertain**: canonical separation of concerns (outbox-driven domain events vs realtime saga pub/sub) vs architectural drift / accidental duplication — needs explicit classification before any consolidation. F0-WRK-2 used `EventDispatcher` because the precedent (`IntegrationEventDeliveryHandler`) is explicit; left `EventService` untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Pre-existing architectural decision, surfaced when F0-WRK-2 needed domain-event subscription.                                                                                                                                                                                                                   | PENDING                                 | Post-fases sweep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-11 | F0-WRK-2 recon                                                              | `apps/workers/src/inboxSyncWorker.ts` uses `prisma.socialMessage.create()` directly (around line 161), bypassing `IngestSocialMessageUseCase` and the aggregate/outbox path. Result: messages ingested via sync (most of them) do NOT emit the `SocialMessageReceived` domain event, so any downstream event-driven handler (triage dispatch, notifications) does not fire for sync-ingested messages. The webhook path correctly goes through the use case. **Status uncertain**: intentional bypass (worker ergonomics, performance) vs canon drift vs forgotten refactor — needs classification. F0-WRK-2's `TriageDispatchEventHandler` covers webhook path; sync coverage is the gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Worker scaffolded before the canonical event-driven ingestion pattern stabilised.                                                                                                                                                                                                                               | RESOLVED                                | RESOLVED (2026-05-29 backlog audit) — `apps/workers/src/inboxSyncWorker.ts` no existe (resuelto en DUP-02 vía Opción B: consumer in-process en apps/api ejecuta `SyncProviderCommentsUseCase` canónico, que sí emite domain events). Sync-ingested messages ahora pasan por el aggregate path.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| SMELL-12 | F0-CLI-2 rewrite                                                            | Semantic inbox classification (`COMPLAINT` / `LEAD` / `QUESTION` / `FEEDBACK` / `SPAM`) used to live as a client-side enum (`InboxMessageType` in the old `useInbox/types.ts`) without any server source. F0-CLI-2 dropped it because the server does not compute it (server `messageType` is the wire type COMMENT / MENTION / REPLY / DIRECT_MESSAGE). If product wants a semantic classification, it must be a new output of the AI triage use case (server side, then exposed on `SocialMessageDTO`). **Status uncertain**: product feature gap vs deliberately abandoned scaffolding.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Inbox UX scaffolding ahead of an AI classification output the triage worker does not produce.                                                                                                                                                                                                                   | CANCELED                                | CANCELED (2026-05-29 backlog audit) — FALSE-POSITIVE. No es smell de código; es feature de producto diferida (clasificación semántica de mensajes vía AI triage). Si el producto la requiere, abre task dedicada con nuevo SMELL ID.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| SMELL-13 | F0-CLI-2 rewrite                                                            | The server's `InboxFilter` (`SocialMessageQueryRepository.ts:44-53`) does NOT accept a `priority` filter, even though `priority` is the principal triage output and the canonical inbox-prioritisation UX wants a "show me URGENT only" pill. The new `InboxSidebar` therefore exposes type/status/platform filters only. To filter by priority server-side, the server's read-side query needs a new optional `priority?: InboxPriority` parameter wired through `GetInboxQuery` and `PrismaSocialMessageQueryRepository`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Read-side filter surface predated the AI triage output.                                                                                                                                                                                                                                                         | PENDING                                 | Future inbox-priority-filter task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| SMELL-14 | F0-CLI-2 rewrite                                                            | The inbox feed exposes only inbound messages — the server does not currently surface an `isOurReply` flag (or equivalent) that lets the client distinguish reply rows we sent from inbound ones. The new `MessageBubble` therefore renders every message as an inbound bubble (left-aligned grey) and drops the previous `direction: INBOUND \| OUTBOUND` axis. Once a server signal lands, the bubble can restore the outbound right-aligned styling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Visual axis carried over from an earlier shape that anticipated `direction` server-side.                                                                                                                                                                                                                        | PENDING                                 | Future server-side outbound-reply-flag task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| SMELL-15 | F0-CLI-2 rewrite                                                            | `apps/client/tests/integration/hooks.integration.test.ts` was a 100% placeholder: 5 `describe.todo` + 22 `it.todo`, zero assertions (commit `85b6b7e`). Audited one-by-one: all 5 referenced hooks are live + used (FORGOTTEN-FEATURE, not DEAD). `useAutoSave` + `useProviders` already had dedicated real tests (10 todos redundant); `useABTests` / `useTemplates` / `useTemplateVersions` (used by TemplateManagementDashboard) had NO coverage. Several todo descriptions didn't match the real hook APIs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | TDD placeholders staged ahead of writing real assertions.                                                                                                                                                                                                                                                       | FIX                                     | 2026-05-22 — placeholder deleted; 3 dedicated MSW v2 integration suites written for the uncovered hooks (useTemplates/useABTests/useTemplateVersions, 15 tests). Client suite now 0 todo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| SMELL-16 | F0-WRK-3 implementation                                                     | Trend-radar multi-source supports per-source filtering via `TrendingDataPort.fetchTrends({ accountId, sources? })` and adapters honour the filter, but there is **no plan-tier / on-demand billing gate** wrapping the `MultiSourceTrendingDataAdapter`. Per Edward's directive (2026-05-20) Perplexity Sonar is intended as a feature plus / on-demand billing add-on; until the gate ships, every account that runs detection consumes Perplexity quota regardless of plan. F0-WRK-3 ships without the gate by design (architecture supports it; policy not yet implemented).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Multi-source trend detection wired ahead of the plan-tier billing matrix.                                                                                                                                                                                                                                       | PENDING                                 | Future billing-gating task (likely Fase 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SMELL-17 | F0-WRK-3 implementation                                                     | `AIServicePort.generateStructured` does not expose Perplexity Sonar's native citation surface (citation position, URL, share-of-voice). `PerplexityTrendingAdapter` workaround: the structured-output schema includes `sourceUrl?: string \| null` and the prompt asks the model to embed citation URLs in the response. Functional but weaker than the native citation field — citations may be hallucinated, share-of-voice cannot be computed. Future canon refinement: extend `AIServicePort` with a citation-aware search method (or a dedicated `WebSearchPort`) that surfaces the provider's native citation structure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Cross-provider AI substrate (`AIServicePort`) generalised before web-search citations existed.                                                                                                                                                                                                                  | PENDING                                 | Future AI-citation-port task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| SMELL-19 | F0-CLI-3 manual E2E smoke test                                              | `apps/client/next.config.mjs` had a global rewrite `{ source: "/api/:path*", destination: "http://localhost:3000/:path*" }` that pre-empted the dedicated route handler `app/api/backend/[...path]/route.ts`. Next.js evaluates rewrites BEFORE the file-system router, so every `/api/backend/<path>` call was forwarded to `http://localhost:3000/backend/<path>` (with `backend/` prefixed) — the API has no `/backend/*` mount, so the response was `Route GET:/backend/<path> not found`. Effect: **every client→API call returned 404 in runtime** (repurpose, inbox, trends, billing, posts, campaigns, ALL hooks). Bug latent through F0-CLI-1 and F0-CLI-2 because unit tests used `vi.stubGlobal("fetch", ...)` and never exercised the proxy. Fixed inline in F0-CLI-3 (rewrite removed; route handler is the single canonical proxy, injects the customer Bearer from the httpOnly cookie). Verdict: FIX (close phase F0-CLI-3 2026-05-20).                                                                                                                                                                                                                                                                                                                                                                                               | Next.js v16 evaluation order quirk + missing manual smoke test in prior client tasks.                                                                                                                                                                                                                           | FIX                                     | F0-CLI-3 (2026-05-20) — global rewrite removed; route handler `[...path]` is the canonical proxy; smoke-test `curl http://omnipost-dev:3200/api/backend/trends/radar` returns 401 (was 404).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| SMELL-18 | F0-API-3 recon                                                              | The legacy trends module (`apps/api/src/trends/trendRoutes.ts` + `TrendAnalysisService.ts` + `TrendReportBuilder.ts` + `trendTypes.ts` + their tests) ships 5 GET endpoints (`/trends/{analysis,viral,opportunities,predictions,report}`) over a `TrendAnalysisService` that talks to a stub TikTok client + circuit breaker and **returns `[]` by default**. Zero callers in `apps/client/` or `apps/admin/`. The module is independent of the F0-WRK-3 `TrendRadarResult` pipeline — it does not consult the table that F0-WRK-3 populates and that F0-API-3's new `/trends/radar` endpoint exposes. F0-API-3 deliberately left the legacy surface intact: chain-deleting 5 endpoints + 1 service + 1 builder + 1 types file + their tests requires its own audit (regla "Enumerate chain-deletes explicitly"). **Status uncertain**: pre-B/F0 forgotten-feature vs scheduled future re-wire.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | TikTok-trend scaffolding pre-B/F0; intended path never materialised — `TrendRadarResult` is the canonical model now.                                                                                                                                                                                            | PENDING                                 | Post-fases sweep (chain-delete task)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| SMELL-20 | F1-API-1 implementation                                                     | The `post_edit` hook (`.claude/hooks-py/post_edit.py`) ran `secretlint` without an explicit `cwd`, inheriting `apps/api` as the working directory. Combined with the `@secretlint/node` patch (`patches/@secretlint__node@12.3.1.patch`, which fixes `node_moduleDir = <cwd>/node_modules`), the rule preset `@secretlint/secretlint-rule-preset-recommend` (installed only in the root `node_modules`) resolved against `apps/api/node_modules` where it does not exist. Effect: secretlint aborted with "rule module not found" and the hook reported it as a leak — a false positive on **every** edit under `apps/api`. lint-staged + `secret:scan` were unaffected (they run from the repo root). Fixed by passing `cwd=PROJECT_DIR` (repo root) to the hook's `subprocess.run`, aligning it with the other invokers; the third-party patch was left intact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Hook authored before the `node_moduleDir` patch made cwd load-bearing.                                                                                                                                                                                                                                          | FIX                                     | F1-API-1 (2026-05-21) — `post_edit.py` runs secretlint from the repo root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SMELL-21 | F1-API-1 implementation                                                     | RAG end-to-end is not yet validated against live providers: all AI provider keys (`OPENAI_API_KEY`, `GEMINI_API_KEY`, `PERPLEXITY_API_KEY`) are empty in `.env` (placeholder comments only), so embeddings cannot be computed and the content LLM cannot run. `GenerateLocalizedContentUseCase` degrades gracefully (brand-voice + locale-native, empty `usedTerms`/`usedRules`); glossary/style-guide upserts persist the row with `embeddingPersisted: false` and a NULL `embedding` column. The grounded-success path (200 + content + cosine retrieval over the HNSW index) is covered deterministically by the trajectory eval with mocked providers, but the live `<=>` cosine search + auto-embedding-on-upsert have not run against real vectors. **Action**: once an OpenAI/Gemini key is configured, re-run `tests/integration/aiLocalizedRoutes.test.ts` and add the grounded-200 assertions.                                                                                                                                                                                                                                                                                                                                                                                                                                              | Embeddings infra shipped ahead of provider-key provisioning.                                                                                                                                                                                                                                                    | PENDING                                 | When an OpenAI/Gemini key is configured (validate RAG end-to-end).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SMELL-22 | F1-CLI-1 implementation                                                     | `apps/admin` uses next-intl in the **cookie-based "without i18n routing"** mode (`apps/admin/i18n/request.ts` resolves locale from the `NEXT_LOCALE` cookie + `Accept-Language`, no `[locale]` route segment, no `proxy.ts` middleware for locale). The canonical next-intl App Router pattern (and the one F1-CLI-1 implemented for `apps/client`) is the `[locale]` dynamic segment + middleware + `generateStaticParams`. Per Edward's directive (canon over consistency, 2026-05-21) the inferior pattern is NOT replicated — `apps/client` follows the canon and `apps/admin` is left as debt to migrate. No URL-localized routes, no static-per-locale generation, no hreflang in admin until migrated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Admin i18n shipped before the `[locale]`-routing canon was applied repo-wide.                                                                                                                                                                                                                                   | FIX                                     | 2026-05-22 — admin migrated to `[locale]` routing (routing/navigation/request/global type-safe, proxy merge createMiddleware+auth, app/ under `[locale]`, switcher via locale navigation, setLocaleAction removed, i18n:lint). Default unified to `en` in both apps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| SMELL-23 | F1-WRK-1 implementation                                                     | The provider `ProviderCapabilities` shape is defined in **four** places that have drifted: the port's inline `capabilities` object (`packages/ports/src/ProviderAdapter.ts`), `packages/providers/shared/src/providerTypes.ts` (`ProviderCapabilities`), `apps/api/src/providers/providerAdapter.interface.ts` (`ProviderCapabilities` + `LegacyProviderAdapter.capabilities`), and `packages/shared/src/providers/providerConfig.ts` (`ProviderCapabilities` + per-provider `PROVIDER_CONFIGS` literals). F1-WRK-1 added the new `mentions` flag only to the port (the runtime contract the worker gates on) + each adapter's `*_CAPABILITIES` const; the other three definitions still lack `mentions`. **Status:** pre-existing duplication, surfaced (not introduced) by F1-WRK-1; consolidating to a single `ProviderCapabilities` type is an architectural task, not inline scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Capability concept duplicated across port / shared / api / config before a single source of truth was established.                                                                                                                                                                                              | PENDING                                 | Post-fases sweep (unify ProviderCapabilities)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SMELL-24 | F1-WRK-1 implementation                                                     | Bluesky mention search (`BlueskyAdapter.searchMentions`) uses the **connected channel's authenticated session** (App Password → `app.bsky.feed.searchPosts`), so brand listening on Bluesky requires a connected Bluesky channel for credential resolution — uniform with the inbox/analytics workers. The F1-WRK-1 plan mentioned the **public AppView** (no auth) as a fallback when no credentials exist; deferred to keep the adapter free of a direct `@atproto` public-agent path and to keep one credential model. **Effect:** a project with tracked terms but no connected Bluesky channel gets no Bluesky mentions. Future: add an unauthenticated public-AppView search path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v1 chose the uniform per-channel credential model over a second (public, no-auth) Bluesky code path.                                                                                                                                                                                                            | PENDING                                 | Future bluesky-public-appview-search task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| SMELL-25 | F1-WRK-1 implementation                                                     | Mention→tracked-term attribution (`matchTrackedTermId` in `apps/workers/src/mentionIngestWorker.ts`) is a case-insensitive **substring** match returning the **first** matching term. Simplifications: (a) a mention matching several terms is attributed to only one (`Mention.trackedTermId` is single-valued); (b) a hit that matched a handle/hashtag synonym not present verbatim in the body gets `trackedTermId = null`. SoV in F1-API-1b can re-classify from `source` (WEBHOOK ⇒ own-brand) + `trackedTerm.kind`, but per-mention multi-term attribution + synonym mapping are not modelled in v1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v1 kept the corpus single-term + substring to ship ingestion before the SoV read model (F1-API-1b).                                                                                                                                                                                                             | PENDING                                 | F1-API-1b (revisit attribution when computing SoV)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SMELL-26 | F1-API-3 implementation                                                     | `apps/workers/src/*` job processors talk to **Prisma directly** instead of resolving application use cases from a DI container (e.g. `mentionIngestWorker.ts`, and the other workers in `apps/workers`). F1-API-3 deliberately did NOT replicate this: its bulk-schedule worker runs **in-process in `apps/api`** and resolves `ProcessBulkScheduleRowUseCase` / `FailBulkScheduleRowUseCase` from `app.container` (DI as premise, zero direct Prisma, no `apps/workers`→`apps/api` dependency), mirroring the repurpose/triage/trend consumers. The existing `apps/workers` direct-Prisma access is pre-existing debt surfaced (not introduced) here — migrating those workers to DI/use-cases is an architectural task, not inline scope. Until then `apps/workers` bypasses the aggregate/UoW/outbox guarantees the use cases enforce.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Workers shipped with direct Prisma access before a DI composition root for `apps/workers` existed.                                                                                                                                                                                                              | PENDING                                 | Post-fases sweep (migrate `apps/workers` processors to DI + application use cases)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SMELL-27 | F2 Bloque B (analytics activation)                                          | **Admin-side analytics never reviewed/activated.** Bloque B scoped strictly to the **client** analytics engine (ROI / cross-platform / performance / realtime / conversions — `apps/api/src/analytics/analyticsRoutes.ts`, all `requireClientAuth`; client dashboard `apps/client/.../dashboard/analytics`). The **admin** platform-operator analytics (`apps/api/src/admin/analyticsRoutes.ts`, `requireAdminAuth` + `requirePermission(ANALYTICS_READ / AUDIT_READ)`; single page `apps/admin/app/[locale]/(dashboard)/analytics/page.tsx`) was deliberately out of B's scope and is **not** present in `FEATURE_TRACE_MATRIX_ES.md` nor any implementation roadmap. Revisit: confirm its endpoints are wired (not 501), the data is real (not mocked), and it surfaces the multi-tenant platform KPIs a platform operator needs (revenue/MRR, account growth, usage aggregates, audit).                                                                                                                                                                                                                                                                                                                                                                                                                                                            | B scoped to the client analytics (product + sales point); admin operative analytics is a separate platform-operator concern.                                                                                                                                                                                    | PENDING                                 | Dedicated admin-analytics review task (post Bloque B)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| SMELL-28 | Homologation (false-green repo tests)                                       | **`Account.subscription` column was dropped (A2 → AccountSubscription) but two production sites still reference it.** (a) `PrismaAccountQueryRepository.updateSubscription` does `prisma.account.update({ data })` where `SubscriptionUpdateData.subscription?: SubscriptionTierKind` — passing `subscription` throws `Unknown argument subscription` at runtime (maxProjects/isOnTrial/trialEndDate are real columns and work). Live caller: `ChangeAccountSubscriptionUseCase.ts:250`. (b) `findById`/`findByEmail` return `account as unknown as AccountDto`, and `AccountDto.subscription` is declared but never populated (always `undefined`). Surfaced while resurrecting the renamed AccountQueryRepository integration test, whose `updateSubscription` block was removed per Edward's call. Decide: drop `subscription` from `SubscriptionUpdateData` + `AccountDto` (+ the use-case path), or reintroduce a real source for the tier.                                                                                                                                                                                                                                                                                                                                                                                                      | The subscription tier moved to the `AccountSubscription` relation; the old `Account.subscription` scalar references were not fully cleaned up.                                                                                                                                                                  | PENDING                                 | Post-fases sweep (subscription-model cleanup)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SMELL-29 | Homologation (false-green repo tests)                                       | **`PrismaAccountRepository` (command repo: findById/findByEmail/save/delete/hardDelete/exists/findAll) has no dedicated integration test.** The file historically named `AccountRepository.test.ts` actually exercised `PrismaAccountQueryRepository` (read side) and was renamed to `AccountQueryRepository.test.ts` during homologation; the command repo's `save`/`delete`/`hardDelete` paths are only covered indirectly by flow tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | The mis-named test file masked the absence of command-repo coverage.                                                                                                                                                                                                                                            | PENDING                                 | Future AccountRepository (command) integration-test task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SMELL-30 | Realtime SSE design (B3)                                                    | **Admin Next proxy buffers SSE — admin webhook dashboard realtime is broken.** `apps/admin/app/api/backend/[...path]/route.ts` reads the full upstream body via `await upstream.text()` (line ~165), so it hangs on infinite SSE streams. The admin `WebhookTimeline.tsx` EventSource (`/api/backend/webhooks/dashboard/stream`) never receives data. Identical root cause + fix as the client proxy (stream `text/event-stream` via `upstream.body` passthrough + `X-Accel-Buffering: no` + `force-dynamic`). The client proxy is fixed in B3; the admin proxy is out of B scope (admin is its own pass — see SMELL-27). Edward confirmed admin must eventually be fixed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The Next proxy was written request/response-oriented (`.text()`); SSE streaming was never accounted for.                                                                                                                                                                                                        | PENDING                                 | Dedicated admin pass (apply the same proxy streaming fix as B3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| SMELL-31 | Realtime SSE appsec review (B3)                                             | **Pre-existing analytics endpoints lack auth/tenancy (cross-tenant read).** Surfaced by the B3 appsec review (adjacent code, NOT introduced here). (a) `GET /analytics/project/:projectId` (`analyticsRoutes.ts`, comment "no auth required for read") has **no preHandler at all** and `getProjectAnalytics` does `project.findUnique({ where: { id } })` with no `accountId` filter → any caller who knows a projectId reads its aggregate analytics. (b) `getDashboard` / other handlers verify the project exists but **not** that `request.customerUser.accountId` owns it, and use `this.prisma` directly in the route (hexagonal/#1 violation). The NEW `/analytics/stream` is tenant-safe (`getProjectAccess(accountId, projectId)` + postId intersection — appsec PASS); these older handlers are not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Analytics routes shipped before the customer tenancy model was enforced; black-box code without guardrails.                                                                                                                                                                                                     | PENDING                                 | Dedicated analytics-tenancy task (add requireClientAuth + getProjectAccess to every analytics handler; route through ports not prisma)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| SMELL-32 | Realtime SSE appsec review (B3)                                             | **No per-account concurrent-SSE-connection cap (DoS surface).** A single authenticated customer can open unbounded `/analytics/stream` (and `/notifications/stream`) connections, each allocating a subscription + a per-connection heartbeat scheduler task. Bounded by `unref()` (won't block shutdown) and customer-auth (not anonymous), but uncapped. Also: the no-`projectId` branch subscribes to every post across all the account's projects (slow connect + large fan-in for big accounts), and half-open TCP sockets rely on heartbeat-write failure / `request.raw.on("close")` for cleanup. `AnalyticsStreamBroadcaster.startHeartbeat` already reserves a no-op tick as the hook for a future stale-subscription sweep. Applies equally to the notifications stream.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | v1 mirrored the existing notifications SSE which also has no cap; connection-limiting deferred.                                                                                                                                                                                                                 | PENDING                                 | Add per-account stream cap (reject in streamRealtime when account exceeds N) + stale-subscription sweep + cap/ require projectId for the no-scope branch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| SMELL-33 | B4 premium-analytics wiring                                                 | **Cross-platform `byProvider` is a `ProviderMetrics[]` array, but the FE `mapCompetitorAnalysis` expects a `Record<string, {avgEngagementRate,totalPosts,topContentTypes,threats}>`.** With empty data (current) the mapper takes the "Industry Average" summary branch (renders real summary-level data — fine). But when `byProvider` is populated, `Object.entries(array)` yields index keys ("0","1") and reads fields (`avgEngagementRate`/`totalPosts`/`topContentTypes`/`threats`) that `ProviderMetrics` does NOT have (it has `provider`/`engagementRate`/`reach`/…), so per-provider competitors render as "0","1" with summary-fallback values. Deeper gap: the Competitive card implies competitor INTELLIGENCE (threats, content strategy, benchmark) that the cross-platform engine does not produce — it only computes per-provider engagement metrics. Align `apiTypes.CrossPlatformApiValue.byProvider` to `ProviderMetrics[]` + rewrite `mapCompetitorAnalysis` to iterate the array (key by `.provider`, rate by `.engagementRate`, reach by `.reach`); leave intelligence fields empty (honest) or source them from a real competitive feature. Needs seeded analytics data to E2E-verify the populated path.                                                                                                                     | The FE mappers were scaffolded against an assumed Record shape before the endpoints returned real data; the engine's array shape diverges.                                                                                                                                                                      | PENDING                                 | Cross-platform per-provider alignment (FE) + decide on real competitor-intelligence source                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| SMELL-34 | B1 audit-spine (prisma→DI #21)                                              | **`AuditableService.writeAuditLog` silently drops `entry.accountId` — account-scoped audit trails are impossible.** `AuditLog` has no `accountId` column, so `logAccountAction` / `logSystemAction` accept an `accountId` but it is neither persisted as a column nor folded into `details`; only `userId` (nullable FK) and `resource`/`resourceId` survive. Effect: you cannot query "all audit events for account X" — a compliance gap for a multi-tenant platform. Pre-existing behaviour (the old prisma-based `writeAuditLog` dropped it too), surfaced — not introduced — while migrating the base to the `AuditLogRepository` port in B1. Fix options: add an `accountId String?` column + index to `AuditLog` (schema migration) and thread it through `AuditLogCreateInput`/adapter, or (cheaper) fold `accountId` into `details` so it is at least searchable in JSON.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Audit schema modelled around user-actor only; account-level actions were bolted on via the `accountId` option that the persistence layer never consumed.                                                                                                                                                        | FIX                                     | RESOLVED S4.3 (2026-05-30) — `AuditLog.accountId String?` column + `@@index([accountId, createdAt])` (migration `20260530003011_add_audit_log_account_id_searchable_column`). Port `AuditLogRepository` extended con `accountId` field + `findByAccount()` method. `PrismaAuditLogRepository` + `InMemoryAuditLogRepository` updated. `AuditableService.writeAuditLog` threads `accountId`. **NOT** in tenant-guard denylist by canon (immutable evidence, MULTI_TENANT_GUARDS.md §Global-tables-denylist + industry research 2026). 14 direct prisma sites tracked como SMELL-47 (DI hygiene, Bucket B).                                                                                                                                         |
| SMELL-35 | B2 audit-spine (prisma→DI #21)                                              | **Brute-force protection no homologada admin↔customer + gap de seguridad de customer-auth.** Tres enfoques divergentes, ninguno canon-completo: (a) `admin/auth/BruteForceProtection.ts` (Prisma, per-userId, lockout durable, threat-score) — cableado en AdminAuthService; (b) `auth/bruteForceProtection.ts` (Redis, per-email+IP, delay exponencial, lockout+IP-block, CAPTCHA, anomaly) — **huérfano, nunca cableado** (forgotten-feature, NO dead — confirmado por auditoría); (c) customer login (`LoginCustomerUseCase` + `/auth/customer/login`) — solo rate-limit de ruta 5/15min, **sin lockout/IP/anomaly** → un atacante rotando IPs contra una cuenta evade el límite. Canon (OWASP Auth Cheat Sheet + NIST 800-63B-4): contador de fallos **por cuenta no por IP**, lockout con auto-expiry/exponencial consciente de DoS, CAPTCHA tras N, MFA. En B2 el huérfano NO se borró: se le inyectó `AuditService` (DI-ready) como base de la homologación. Diseño completo en `docs/security/BRUTE_FORCE_HOMOLOGATION_ES.md`.                                                                                                                                                                                                                                                                                                                | Auth de admin y customer evolucionaron por separado; la impl Redis completa se construyó y nunca se cableó.                                                                                                                                                                                                     | FIX                                     | RESOLVED S4.4 (2026-05-30) — `BruteForceProtectionPort` + `RedisBruteForceAdapter` canon-aligned (NIST SP 800-63B-4 + OWASP Auth Cheat Sheet). Wired en `AdminAuthService.login` y `LoginCustomerUseCase` via DI composition root. Customer gap cerrado: account-based primary counter (IP rotation NO bypassa), exponential backoff 1s→300s, CAPTCHA threshold=3, lockout=10 con auto-expiry 30min, fail-open con metric `bf_adapter_failure` (anti-DoS). 845 LOC dead/divergent eliminados (huérfano + admin-specific). 15 unit tests + InMemoryBruteForceAdapter helper. Decisión grabada en [ADR-0015](../technical/ADR-0015-brute-force-protection-port-canon.md). Cross-link SMELL-37 parcial (AdminAuthService ya no construye BF inline). |
| SMELL-36 | B3 templates (prisma→DI #21)                                                | **`TemplateService` is a "Control Freak" (Seemann) — it constructs its 3 collaborators inline instead of receiving them.** `templateService.ts` ctor does `new TemplateVersionService(this.prisma)`, `new TemplateABTestService(this.prisma)` and (added in B3) `new ServerTemplateEngine(this.prisma)`. All three are Volatile (DB-touching) dependencies; per Seemann's Control Freak anti-pattern + CLAUDE.md §DI L464 they should be injected. B3 was scoped to #21 (killing the `templateService` + `serverTemplateEngine` module singletons) and, per Edward's call, kept the inline construction consistent with the file's existing pattern rather than expanding scope. `TemplateService` is `@layer infrastructure` so CLAUDE.md L459 ("no `new` in domain/application ctors") does not strictly bind it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Sub-services constructed inline since genesis; #21 only removed the module-level eager singletons, not the intra-class wiring.                                                                                                                                                                                  | RESOLVED                                | RESOLVED (2026-05-29 backlog audit) — `apps/api/src/templates/templateService.ts` no existe (clase removida en refactor posterior; templates ahora viven en `packages/core/style-guide/` o equivalente). Verdict cleanup pendiente al cierre.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SMELL-37 | B4 admin services (prisma→DI #21)                                           | **`AdminAuthService` is a "Control Freak" + writes audit off-spine.** Its ctor constructs 4 collaborators inline (`new PasswordService(this.prisma)`, `new SessionManager(...)`, `new BruteForceProtection(this.prisma)`, `new MfaService(this.prisma)` — the admin-specific MfaService/BruteForceProtection) instead of injecting them; and `AdminAuthService` writes audit via `this.prisma.auditLog.create` directly (line ~508) rather than through `AuditService`/`AuditLogRepository` (it doesn't extend AuditableService). B4 was scoped to #21 (killing the `adminAuthService` module singleton → container factory + middleware resolves via `request.server.container`), keeping the inline construction per the B3 precedent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Admin auth stack composed inline since genesis; #21 only removed the eager module singleton.                                                                                                                                                                                                                    | PARTIAL FIX                             | PARTIAL S4.4 (2026-05-30) — `BruteForceProtection` parte cerrada vía [ADR-0015](../technical/ADR-0015-brute-force-protection-port-canon.md): `AdminAuthService` ya no construye `new BruteForceProtection(this.prisma)` inline, ahora recibe `BruteForceProtectionPort` via DI. **PENDING**: las otras 3 colaboradoras inline (`PasswordService`, `SessionManager`, `MfaService`) + audit off-spine (`this.prisma.auditLog.create` directo) siguen como deuda Bucket C; refactor dedicado al cerrar IR.                                                                                                                                                                                                                                           |
| SMELL-38 | B7 webhooks (prisma→DI #21)                                                 | **Inbound webhook ingestion pipeline is DI-ready but never wired (forgotten-feature, NOT dead).** 11 of 13 webhook files form a complete inbound pipeline — `WebhookManager` → `createWebhookJobProcessor` → `WebhookJobProcessor` → `UniversalWebhookHandler` → 8 provider processors — that parses, dedupes, HMAC-verifies (with rotation grace), persists `webhookEvent`, updates publishLog/post/analytics, feeds the LIVE `RealtimeWebhookBroadcaster` (SSE) and enqueues the mention-ingest worker. It is fully unit-tested (≈600 tests) yet has NO construction site: there is no inbound HTTP route and no worker bootstrap, so no provider event ever reaches it. Two known wiring gaps: (a) `webhookJobProcessor.ts` builds the handler with `broadcaster=undefined` so the SSE broadcast path is dark even once wired; (b) `WebhookManager` is not registered in any composition root. B7 threaded `PrismaClient` through the whole cascade (constructor injection, #21 17→4) and left every class DI-ready; the actual cabling was deliberately deferred per Edward (DI now, wire later). Full design in `docs/features/WEBHOOK_INGEST_WIRING_ES.md`.                                                                                                                                                                                     | Pipeline built end-to-end with tests but the inbound route + worker bootstrap were never added; the broadcaster was never passed into the job processor.                                                                                                                                                        | PENDING                                 | Workstream WEBHOOK-INGEST (inbound POST/GET `/webhooks/:provider` route with Meta challenge + per-provider signature verify, register WebhookManager in a composition root, pass RealtimeWebhookBroadcaster into the handler, appsec replay/timing review, E2E)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| SMELL-39 | B8 workers (prisma→DI #21)                                                  | **Workers reimplement application use-cases inline against prisma instead of resolving the shared core (logic duplication).** `analyticsIngestWorker.processJob` duplicates `IngestChannelAnalyticsUseCase` (whose JSDoc literally says "Called by the analytics ingestion worker" — but the worker never calls it); `inboxSyncWorker.processJob` duplicates `SyncProviderCommentsUseCase`/`IngestSocialMessageUseCase` and is a DEGRADED copy (omits the conversation-grouping the use-case does). Root cause: the application core lives in `apps/api/src/application` and is NOT importable from `apps/workers` (no shared `packages/@core`), so workers cannot resolve the canonical use-cases and reimplement them. B8 injected PrismaClient into the workers (DI, #21 4→0); the duplication itself was then resolved via Option B (see verdict). Full registry: `docs/reports/code-duplications.md` (DUP-01, DUP-02). Supersedes/extends SMELL-26 (workers talk to Prisma directly).                                                                                                                                                                                                                                                                                                                                                            | apps/api and apps/workers evolved as separate deployables; the application layer was never promoted to packages/, so the worker copies were written inline and drift from the canonical use-cases.                                                                                                              | DUP RESOLVED (logic); core-epic PENDING | Logic duplication RESOLVED via Option B: the analytics/inbox consumers now run in-process in apps/api over the canonical use-cases (`analyticsIngestConsumer.ts` / `inboxSyncConsumer.ts`); the two worker `processJob` copies were deleted (`docs/reports/code-duplications.md` DUP-01/02 = RESUELTAS). REMAINING (separate epic, not a duplication): promote the application core to `packages/@core` so any deployable resolves the same use-cases and the consumers can return to an isolated process if desired — designed in `docs/architecture/TARGET_ARCHITECTURE_CANON_ES.md`                                                                                                                                                            |
| SMELL-40 | A3 recon (application→@core)                                                | **`dependency-cruiser` rule `core-application-no-infrastructure` is a partial guard — it does not catch `@packages/api-common`.** Its `to.path` enumerates only `prisma\|fastify\|ioredis\|bullmq\|next\|@fastify\|@prisma\|@infra\|@adapters`, so a `@core/application` file importing `@packages/api-common` (classified `@layer infrastructure`; e.g. `reports/GenerateReportUseCase` using `exportToCSV`/`ColumnDefinition`) does NOT trip the boundary gate. A future clean-phase move could smuggle an infra dependency into the core unnoticed; only the canon currently blocks it (which is why A3 deferred `GenerateReport` to A6 by hand rather than relying on the gate).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The rule was written enumerating the framework/adapter packages known at @core P0; `api-common` (a mixed framework-neutral + framework-coupled helper package) was never added to the deny list.                                                                                                                | RESOLVED                                | RESOLVED A6.7 — `exportToCSV` (pure RFC 4180 serializer) relocated to `@shared` (barrel `@shared/types`); api-common re-exports it (8 consumers incl. admin frontend intact); `reports/GenerateReportUseCase` moved to `@core/application`; `@packages/api-common` added to the rule's `to.path` (probe-verified the guard now trips). Canon: a pure function ≠ a port — relocate to the shared kernel, not wrap in a port.                                                                                                                                                                                                                                                                                                                       |
| SMELL-41 | A6.1 recon (application→@core)                                              | **Two use-cases are unwired (forgotten-feature, NOT dead).** `custom-reports/EnableReportSharingUseCase` has a DI token (`TOKENS.EnableReportSharingUseCase`) but **no `container.register` binding** and no `resolve` site; `notifications/SendEmailNotificationService` has **no token, no construction, no resolve** at all. Both are fully unit-tested yet reachable only from their tests — not part of any live request/worker path. A6.1 refactored EnableReportSharing (config injection + move) and A6.4 will move SendEmailNotificationService, but neither sub-phase wires them (wiring is out of scope and needs its own audit).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Use-cases built + tested ahead of (or after losing) their construction site; the DI binding / consuming route was never added.                                                                                                                                                                                  | PENDING                                 | Dedicated wiring task (register EnableReportSharing on a custom-reports route; call SendEmailNotificationService from the notification-creation flow) or WONT_FIX if abandoned — decide in the post-fases sweep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| SMELL-42 | P8c recon (depcruise full-repo)                                             | **Pre-existing circular dependency in `apps/workers`** (`no-circular`, ERROR): `publishHandlerTypes.ts ↔ telemetry/initialization.ts`. The CI depcruise scope (`apps/api/src packages/core packages/shared`) does not scan `apps/workers`, so this is not gated today. Surfaced while verifying the P8c core-\* flip (full-repo run).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Module cycle in the workers bootstrap/telemetry wiring.                                                                                                                                                                                                                                                         | RESOLVED                                | RESOLVED P9c — moved the 4 cycle types (`ContentMetrics` + `PublishInstrumentation`/`DatabaseInstrumentation`/`BusinessKPITracker`) to a leaf module `apps/workers/src/telemetry/instrumentationTypes.ts`; repointed the 3 consumers. depcruise `apps/workers/src` → 0 violations (no-circular gone). Type-only relocation, no runtime change.                                                                                                                                                                                                                                                                                                                                                                                                    |
| SMELL-43 | P8c recon (depcruise full-repo)                                             | **19 `no-orphans` (warn) in `apps/admin`** (e.g. `app/[locale]/loading.tsx`, `app/[locale]/(auth)/layout.tsx`) — Next.js App Router special files flagged as orphan modules. Cosmetic (warn), not gated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Next.js convention files (loading/layout) have no explicit importer; depcruise cannot see the framework’s implicit usage.                                                                                                                                                                                       | PENDING                                 | Post-fases sweep — add a Next.js special-file exception to the `no-orphans` rule, or WONT_FIX.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| SMELL-44 | P9b recon (workers→@core)                                                   | **`ChannelAuthFailureRecorder` (apps/workers) overlaps `UpdateChannelAuthStateUseCase` (@core).** Both set the same 3 channel auth-failure fields (`needsReauth`/`authFailedAt`/`authFailureReason`); the worker service hand-rolls the channel update + a `ChannelAuthFailed` outbox event in a raw `$transaction`, while the @core use-case uses `channel.markForReauth()` + UoW. NOT a clean duplicate: different triggers (worker auto-detection on provider auth error vs admin force-reauth via route) and the recorder ALSO emits `ChannelAuthFailed` (provider+detectedAt) which `markForReauth()` does not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The worker needed a self-contained auth-failure recorder before the @core use-case existed; convergence was never done.                                                                                                                                                                                         | PENDING                                 | Promote `Channel.markForReauth()` to emit a canonical `ChannelAuthFailed` domain event (+ verify a consumer exists), then have the worker call a @core use-case instead of raw prisma. Bigger than a reuse — needs the aggregate event first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| SMELL-45 | S2 cleanup audit (F11)                                                      | **`packages/core/threading` es GENESIS-SCAFFOLDING-HALF-WIRE — backend 5/5 funcionando, UI dashboard de threads ausente.** Backend completo: `threadPlanner.planThread()` consumido por `XAdapter.render()` + `_template/`; workers `publishHandler.publishThreadPost()` orquesta publishing usando el `ThreadPlan`; modelos `Thread` + `TweetFragment` + enum `ThreadStrategy` en Prisma; API `/analytics/threads/:id/performance` existe. UI: client tiene `twitterThreadToggle` (E2E test pasa) + preview que calcula `willThread`. Gap: NO hay dashboard de threads publicados ni viewer de thread analytics en admin/client. Plus el package está documentado en `docs/architecture/TARGET_ARCHITECTURE_CANON_ES.md:99,106,178` como "isla ad-hoc" candidata a absorber en `@core/domain`. Aplicada la regla canon `~/.claude/feedback/audit-deletion.md §3-preguntas + §orphan-as-signal`: 5/5 eslabones backend OK, 0 duplicación, NO DELETE.                                                                                                                                                                                                                                                                                                                                                                                                  | Scaffolding del split §5.1.a (Genesis 2026-03-08, commit `5603d6be`), pensado para absorberse en `@core/domain` organizado.                                                                                                                                                                                     | PENDING                                 | Dedicated thread-monitoring-dashboard task (UI client + admin viewer) + post-fases sweep para decidir absorption en `@core/domain` (cross-ref TARGET_ARCHITECTURE_CANON_ES).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| SMELL-46 | S2 cleanup audit (F13+F14)                                                  | **2 hooks TanStack de saga sin UI consumer (DORMANT INFRASTRUCTURE).** `apps/client/lib/hooks/useSagaStatus.ts` (F13, polling 1s sobre `GET /sagas/:sagaId` hasta terminal) + `useStartPostPublishingSaga.ts` (F14, mutation sobre `POST /sagas/post-publishing/start`) fueron creados en commits `6c4651c5` + `55c2a59e` (2026-05-09) como rama declarativa del flow saga "for UI loading states" (literal del commit message). 0 callers en `apps/admin` + `apps/client`. Backend completo: `SagaInstance` Prisma model, `SagaIntegration.ts` con rutas `/sagas/*`, `PostPublishingSaga` definition en `packages/shared/src/saga.ts:857`. La rama imperativa `runSagaAndAwaitTerminal()` cubre los 6 callers reales (preview, editor, scheduling). El componente `<SagaProgress>` que debería montar estos hooks **nunca se creó** — candidato natural en `SchedulingDashboard`. Aplicada regla canon: NO DELETE (FORGOTTEN-UI-WIRE). Registrado adicionalmente en `code-duplications.md` DUP-03 (KEEP-both, semánticas distintas).                                                                                                                                                                                                                                                                                                                 | Bifurcación declarativa/imperativa del flow saga ahead de UI component: la rama imperativa se adoptó, la declarativa quedó esperando su consumer (un `<SagaProgress>` en SchedulingDashboard).                                                                                                                  | PENDING                                 | Dedicated saga-monitoring-dashboard task (crear `<SagaProgress>` que monte los 2 hooks + integrar en `SchedulingDashboard`/preview) o WONT_FIX si product abandona la UI real-time de saga.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| SMELL-47 | S4.3 surfaced (SMELL-34 fix)                                                | **14 sitios bypass del `AuditLogRepository` port — escriben/leen `prisma.auditLog.create()` / `findMany()` directamente.** Sites confirmed (`grep prisma\.auditLog\.create\|findMany apps/api/src`): admin/AnalyticsComplianceHandlers (1), admin/AnalyticsAccountHandlers (1), admin/AnalyticsDashboardHandlers (1), admin/auth/AdminAuthService (2), security/credentialManager (1), security/auditLogger (3), audit/auditService (3), audit/activityFeedService (1), billing/gatewaySwitchProcessor (1) = 9 files / 14 sites. DI hygiene gap, NOT security urgency: AuditLog stays outside RLS by canon (immutable evidence, MULTI_TENANT_GUARDS.md §Global-tables-denylist), so bypass doesn't cause cross-tenant leak. Pero threading de `accountId` desde estos sites es manual hasta migración al port (S4.3 introdujo column + port surface).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Pre-existing code escribió/leyó AuditLog directo a `prisma` antes de que el port `AuditLogRepository` + servicios que extienden `AuditableService` existieran. Refactor hacia port parcial.                                                                                                                     | PENDING                                 | Sprint dedicado DI-cleanup post-IR: migrar los 9 files (14 sites) a `AuditLogRepository.create()` + `findByAccount()` + `findByUser()`. Bucket B (IR-dependency — affects admin auth workstreams).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| SMELL-49 | ci-failures-repair (RC-4 Option A, 2026-06-04)                              | **All 48 `packages/core/*` packages should migrate to real TypeScript project references (`composite: true` + `references` array + `paths`→`dist` + real emit).** Current state after ci-failures-repair Option A: all 48 packages use `{ noEmit: true }` (45 fixed by this workstream; domain/application/mentions already had it). This is correct for a typecheck-only build but NOT the textbook monorepo setup. Real TS project references would: (a) allow incremental compilation (tsc `--build` via `--composite`), (b) emit declaration files for proper IDE navigation across packages, (c) enforce build-order via `references` graph instead of path-aliases to source, (d) remove the `tsconfig.base.json` `@core/*`→src hack which is the root cause of TS6059. Root cause of current state: two scaffold events used different templates — `2d9418ac` (domain/application, noEmit) vs `431b8667` (46 bounded-context packages, rootDir/outDir). The dual-scaffold trap was hidden because `turbo run build` is fail-fast and only surfaced mentions.                                                                                                                                                                                                                                                                                   | Architectural monorepo migration. High risk: touches build order across 48 packages (each needs `composite: true`, `declaration: true`, `declarationDir`, `references` to each dep, and consumers need `paths` updated to `dist/` not `src/`). Requires its own ADR. Not doable as a CI-repair incremental fix. | PENDING                                 | Dedicated workstream: migrate all 48 @core packages (plus @ports/_, @shared/_) to full TS project references. Define scope/ADR first. Option A (noEmit uniform across 48) is the stable CI-green baseline until this is done. Reference: ci-failures-repair commits 626d78d8 (Option A fix) + discovery in engram topic `discovery/core-tsconfig-noemit-scaffold`.                                                                                                                                                                                                                                                                                                                                                                                |
| SMELL-48 | channel-canonical merge → main (size-limit CI, 2026-06-03)                  | **admin client bundle excede el size-limit (1.2MB gzip) por ~108KB tras mergear el catch-up a main.** El crecimiento NO viene de `apps/admin` (solo recibió commits comment-only en el rango) sino de los packages compartidos que el catch-up engrosó (`@core`/`@ports`/`@shared` — RateLimiterPort, BruteForceProtectionPort, AI resilience, etc.) que el bundle **cliente** de admin arrastra transitivamente. Olor real: ¿por qué el bundle cliente de admin incluye código backend-ish compartido? Posible polución de bundle (imports server-only llegando al cliente). Interim aplicado (commit en `workstream/post-merge-ci-fixes`): límite admin bumpeado 1.2→1.35MB para restaurar verde — el crecimiento es real/acumulado del catch-up, NO regresión del rediseño bulk. Medir/reducir requiere build de admin (riesgoso en el LXC capado). **Ampliación 2026-06-03:** el bundle CLIENTE también excede (1.67 vs 1.4MB, +266KB) — parte es el rediseño (PR3 importa `CSVBulkUpload` ESTÁTICO en `BulkScheduleView`, arrastrando PapaParse al bundle), parte catch-up. CLAVE: el size-limit mide el AGREGADO de todos los chunks → `next/dynamic` NO reduce el agregado (solo mejora el initial-load); el gate se arregla con bump o quitando/aligerando deps. Interim: límite client bumpeado 1.4→1.75MB.                                  | Shared packages (`@core`/`@ports`/`@shared`) crecieron en el catch-up; el bundle cliente de admin los arrastra.                                                                                                                                                                                                 | PENDING                                 | Dedicated admin-bundle-audit task: analizar qué arrastra el bundle cliente de admin desde los packages compartidos (¿server-only leaking?), aplicar dynamic-import / tree-shake, y bajar el límite de vuelta a ~1.2MB. (client) `next/dynamic` para `CSVBulkUpload` (perf initial-load) + reducir el AGREGADO cliente (parser CSV más liviano que PapaParse / audit de deps) y bajar el límite client de vuelta a ~1.4MB.                                                                                                                                                                                                                                                                                                                         |
| SMELL-51 | knip-at-alias-resolution (oxc-resolver 11.20.0 false-positives, 2026-06-17) | **FORGOTTEN-FEATURE: admin audit-logs + content-library pages pending product decision.** `apps/admin/hooks/api/useAuditLogs.ts` and `apps/admin/hooks/api/useContentLibrary.ts` surfaced as knip "unused files" only because a knip `@/`-alias resolution false positive (oxc-resolver 11.20.0 correctly refused to let the test-excluding leaf tsconfig own a colocated `*.test.tsx`, and there is no root `tsconfig.json` owner) severed their last live importer — their tests. The knip-layer fix (`knip.json` `paths` `@/*`→`./*` for apps/client + apps/admin) cleared the false positive; these two hooks are **NOT dead code**. 3-questions + feature-surface audit (`~/.claude/feedback/audit-deletion.md`) → FORGOTTEN-FEATURE: (a) `useAuditLogs` — backend **fully implemented** (`apps/api/src/audit/auditRoutes.ts` + Prisma `model AuditLog` + `auditLogRetentionDays` config); hook + tests written; **only** missing an admin page to wire it. (b) `useContentLibrary` — calls the real `/api/backend/posts` endpoint; hook + tests written; **only** missing an admin page. Both originate from the Genesis commit (`5603de6b`, 2026-03-08) = intentional scaffolding. Correct action = WIRE the admin pages OR keep tracked — a **product** decision (Edward), **never** an automatic delete. Nothing deleted by the knip change. | Genesis-commit scaffolding: audit-log + content-library admin UI staged ahead of their wiring; their backends/hooks/tests exist, only the admin pages are missing.                                                                                                                                              | PENDING                                 | Product decision (Edward): wire the admin audit-logs + content-library pages OR keep tracked as pending-wire. Do NOT delete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| SMELL-52 | dev-memory OOM (Turbopack dev RSS, 2026-06-19)                              | **Frontend dev runs webpack instead of Turbopack (Next 16's default) on the 9GB homelab box to avoid recurrent OOM.** Turbopack dev RSS hit ~3.0GB for `apps/client` alone (admin ~0.4GB) leaving only 239MiB free; switching `apps/admin` + `apps/client` `dev` scripts to `next dev --webpack` dropped each frontend to ~0.3-1.5GB and restored 2.5GiB free (6.1→4.3GB used). Research (workflow `wf_204ad24f`, Next 16): NO dev memory cap exists (`turbopackMemoryLimit` was removed in v16), `--max-old-space-size` does NOT bound Turbopack (Rust engine), and multi-GB Turbopack dev RSS is normal (~1.5-2.5× webpack). The ~3GB is the app's compiled module graph, NOT the `turbopack.root` scope (vercel/next.js #81161). Trade-off accepted: webpack HMR is slower. Production build untouched (still Turbopack/build pipeline).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Deliberate dev-env trade-off to fit the capped homelab LXC — not a code defect.                                                                                                                                                                                                                                 | DEFERRED (revert-when-RAM-allows)       | Revert to Turbopack — drop `--webpack` from the two `dev` scripts — when the dev box has more physical RAM. Deeper levers researched (workflow `wf_204ad24f`): `turbo run dev` collapses ~1.4GB of nested pnpm wrappers into one process (applied alongside); GVS→repo-scoped `turbopack.root` (disable pnpm `enableGlobalVirtualStore`) as runaway-OOM insurance (#92978/#93556) — layout-only, version-safe, deferred.                                                                                                                                                                                                                                                                                                                          |

## Bucket Classification (2026-05-29 audit)

Backlog audit pre-Implementation Roadmap. Cada SMELL fue evaluado con las 3 preguntas canon (`~/.claude/feedback/audit-deletion.md`) + flow analysis. Verdicts revisados; clasificación tri-bucket:

- **A — Security/blocker crítico**: OWASP, cross-tenant, runtime gap, DoS, security gap real. Atacar pre-IR (workstream S4).
- **B — IR-dependency**: smell que afecta diseño de un workstream del Implementation Roadmap; cerrar cuando IR toque su área.
- **C — Cosmético/tech-debt menor**: refs sprint en comments, warns no-gated, type smells locales sin impacto runtime. Post-IR sweep.

| Bucket                                     | Count | SMELLs                                                                                                                                                       |
| ------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **A** (security pre-IR)                    | 4     | SMELL-31, SMELL-32, SMELL-34, SMELL-35                                                                                                                       |
| **B** (IR-dependency)                      | 16    | SMELL-3, SMELL-9, SMELL-10, SMELL-13, SMELL-14, SMELL-16, SMELL-17, SMELL-23, SMELL-24, SMELL-26, SMELL-27, SMELL-30, SMELL-38, SMELL-41, SMELL-45, SMELL-46 |
| **C** (cosmético/tech-debt)                | 14    | SMELL-1, SMELL-2, SMELL-5, SMELL-7, SMELL-8, SMELL-18, SMELL-21, SMELL-25, SMELL-28, SMELL-29, SMELL-33, SMELL-37, SMELL-43, SMELL-44                        |
| **ALREADY-RESOLVED** (verdict actualizado) | 11    | SMELL-4, SMELL-6, SMELL-11, SMELL-15, SMELL-19, SMELL-20, SMELL-22, SMELL-36, SMELL-39, SMELL-40, SMELL-42                                                   |
| **CANCELED (FALSE-POSITIVE)**              | 1     | SMELL-12                                                                                                                                                     |
| **TOTAL**                                  | 46    | —                                                                                                                                                            |

**Edward decision (2026-05-29)**: atacar TODO Bucket A en sprint S4 pre-IR. Plan: `docs/audits/PLAN_REPARACION.md §S4` (cuando se agregue) o issue dedicado.

**Bucket B items** se atacarán durante Implementation Roadmap cuando los workstreams correspondientes los toquen — cada workstream IR incluye su(s) SMELL(s) B. **Bucket C** se cierra en post-IR sweep dedicado.

---

## Notes

- F0-API-1's own new code follows the fail-loud canon
  (`tests/integration/repurposeRoutes.test.ts`) and the typed
  `request.customerUser` accessor — it does **not** reproduce SMELL-1 or
  SMELL-2.
- This backlog is fed continuously; absence of an entry is not evidence
  of absence — the Bloque B revisit (post-Fase 0) is a scheduled pass.
- **AUDIT-1 pass (2026-05-19)** — dedicated read-only audit over the 72
  `.ts/.tsx` files touched by B1/B2/B3/B5 + F0-WRK-1/F0-API-1/F0-CLI-1.
  Verified with `git blame` per finding; fitness greps scoped to the
  perimeter were 0 across canon checks. By Edward's call SMELL-5..8
  include pre-existing tech debt **surfaced while editing the file**
  even though it was not introduced by B/F0 — the alternative (leaving
  the spotted debt only in the audit transcript) is a soft time bomb.
  Origin "AUDIT-1 (pre-existing, surfaced)" makes the provenance
  explicit so the post-fases sweep can route them to whichever
  workstream they actually belong to.
