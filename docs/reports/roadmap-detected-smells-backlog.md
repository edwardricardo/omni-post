# Roadmap — Detected Smells Backlog

Living, trackable log of smells / anti-patterns / dead-code detected
**during** a roadmap task but **outside that task's scope**. Findings are
neither fixed inline (anti-churn) nor hidden (anti-time-bomb): they are
recorded here with a pending verdict and closed in a dedicated cleanup
sweep **after all phases are complete**.

Process:

- A finding outside the current task's scope → add an entry here, mention
  it, continue the task strictly scoped. New code still follows current
  canon so no new time bombs are introduced.
- After **Fase 0** closes: revisit the code from the Bloque B iterations
  and feed this backlog with findings.
- The **cleanup sweep** runs after **all phases** close, item-by-item,
  with the 3-question filter (origin + purpose + duplication) and an
  explicit verdict (FIX / DEFER / WONT_FIX).

Verdict legend: `PENDING` (not yet adjudicated) · `FIX` · `DEFER` ·
`WONT_FIX`.

| ID       | Origin                                | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Original purpose                                                                                                                               | Verdict | Close phase                                                                                                                                                                                                                                                          |
| -------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SMELL-1  | F0-API-1 recon                        | Old integration tests use the skip-if-API-down anti-pattern (`apps/api/tests/integration/crisisRoutes.test.ts` et al.: `console.log("⚠️ … will be skipped")` + early return) instead of the fail-loud canon used by `sagaCustomerFlow.test.ts` (`assert.ok(apiAvailable, …)`). Effect: green CI that silently tested nothing — a latent time bomb.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Defensive test scaffolding from before the fail-loud canon was adopted.                                                                        | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-2  | F0-API-1 recon                        | `apps/api/src/custom-reports/customReportRoutes.ts:getAccountId()` casts `request as unknown as Record<string, unknown>` to read `request.user?.accountId` instead of the typed `request.customerUser?.accountId` exposed by `requireClientAuth`. Untyped accessor; inconsistent with the canonical customer-auth shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Legacy accessor predating the typed `CustomerRequestUser` augmentation.                                                                        | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-3  | F0-CLI-1 recon + AUDIT-1              | Repurpose variant approve/reject UI was removed from `apps/client/app/dashboard/ai/repurpose/page.tsx`: it was wired to the generic `/api/backend/approvals/:id/:action` endpoint (not repurpose-specific) and assumed a nested `variants[]` shape the real DTO does not carry. The backend half is the matching forgotten-feature: `ApproveRepurposeVariantUseCase`/`RejectRepurposeVariantUseCase` are registered in DI (`apps/api/src/infrastructure/container/setupRepurposeUseCases.ts:54-72`) with their Prisma adapters (`PrismaApproveVariantAdapter`, `PrismaRejectVariantAdapter`) but no route consumes them. Closing this means a dedicated repurpose-approve/reject API task plus the matching UI.                                                                                                                                                                                                                                         | Variant-management surface scaffolded ahead of its backend contract.                                                                           | PENDING | Future repurpose-approve task / post-fases                                                                                                                                                                                                                           |
| SMELL-4  | F0-CLI-1 recon                        | AI sub-pages (`/dashboard/ai/optimizer`, `/ai/trends`, `/ai/repurpose`, `/ai/templates`, `/ai/analytics`) are not reachable from the dashboard sidebar — the nav has a single `AI` entry pointing only at `/dashboard/ai/generate` (`apps/client/app/dashboard/layout.tsx`). Global, pre-existing information-architecture gap, not repurpose-specific.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Sidebar predates the AI sub-page expansion.                                                                                                    | FIX     | F0-CLI-3 (2026-05-20) — sidebar AI collapsible sub-menu with 6 entries (Generate / Trends / Repurpose / Optimizer / Templates / AI Analytics); auto-expands on `/dashboard/ai/*` routes; aria-expanded toggle.                                                       |
| SMELL-5  | AUDIT-1 (B1, commit 65b84c7)          | `apps/api/src/ai/structuredSchemas.ts` repeats the pattern `jsonSchema: z.toJSONSchema(<schema>) as Record<string, unknown>` **four** times (lines 120, 129, 136, 149). The cast widens `z.toJSONSchema()`'s precise return type to an opaque `Record`. Either the consumer's `StructuredOutputSpec.jsonSchema` field type should narrow to match zod's actual return, or the cast site should narrow — currently the boundary is fuzzy in both directions and the fitness #3 grep does not catch it (it is not `as any`).                                                                                                                                                                                                                                                                                                                                                                                                                              | Cross-provider structured-output glue between zod and SDK shapes built in B1.                                                                  | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-6  | AUDIT-1 (pre-existing, surfaced)      | `apps/api/src/infrastructure/container/types.ts:147,150,153,156` carries task-code references in comments (`P3-A — publish flow integration`, `P1-DI-7 — link tracking`, `P1-DI-8 — crisis mode`). Violates the comment-quality rule "no references to sprint numbers, implementation phases, or development timeline". Pre-B/F0 (genesis commit `^5603d6b`), spotted while F0 commits added new symbols to the same file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Provenance breadcrumbs from an earlier sprint-tracked refactor.                                                                                | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-7  | AUDIT-1 (pre-existing, surfaced)      | `apps/api/src/index.ts:883` uses a double cast `(app as unknown as Record<string, unknown>).sagaIntegration as ...` to read the saga decorator off the Fastify instance, instead of augmenting the `FastifyInstance` module to declare the property. Type smell of the worst class (bypasses two safety nets at once). Pre-B/F0 (commit `069155c5`), spotted while F0 wired the repurpose consumers into the same shutdown block.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Quick saga-integration getter ahead of a proper Fastify decorator typing.                                                                      | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-8  | AUDIT-1 (pre-existing, surfaced)      | `apps/api/src/ai/orchestrator.ts:58,60` casts `(value as Record<string, unknown>)` inside `stableStringify` after the function has already narrowed `value` to `typeof === "object" && !== null && !Array.isArray`. The cast is replaceable with a proper type guard so TypeScript narrows without `as`. Pre-B/F0 (commit `b06a189e`), spotted while B1 edited the surrounding orchestrator surface.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Determinism helper for AI call caching, written ahead of strictest type rigor.                                                                 | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-9  | F0-WRK-2 recon                        | `apps/api/src/application/inbox/handlers/InboxEventHandlers.ts` is registered in DI (`setupInboxUseCases.ts:236-243`) but **never subscribed** to `EventDispatcher`. `onSocialMessageReceived` (creates inbox notification) and `onSocialMessageReplied` have **zero callers** in `apps/api/src`. The notification side of inbox event handling is dormant — domain event `SocialMessageReceived` is emitted by `SocialMessageAggregate` but the handler that should create notifications never runs. **Status uncertain**: dead code, forgotten-feature awaiting subscription wiring, or scheduled for a future task — needs classification.                                                                                                                                                                                                                                                                                                           | Notification routing on message arrival, scaffolded ahead of its subscription/dispatch wiring.                                                 | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-10 | F0-WRK-2 recon                        | Two parallel event-dispatch systems coexist in the repo: `EventDispatcher` (`apps/api/src/domain/events/DomainEvent.ts:67-78`, outbox-driven, `DomainEventHandler<T>` interface, used by `IntegrationEventDeliveryHandler`) and `EventService`/`sagaEventService` (`apps/api/src/events/EventService.ts`, Redis pub/sub, `EventHandler` from `@shared/events`, used for `EVENT_TYPES.POST_PUBLISHED`/`USER_ACTION`/`SYSTEM_HEALTH`). The two dispatch different event catalogs with different handler contracts. **Status uncertain**: canonical separation of concerns (outbox-driven domain events vs realtime saga pub/sub) vs architectural drift / accidental duplication — needs explicit classification before any consolidation. F0-WRK-2 used `EventDispatcher` because the precedent (`IntegrationEventDeliveryHandler`) is explicit; left `EventService` untouched.                                                                          | Pre-existing architectural decision, surfaced when F0-WRK-2 needed domain-event subscription.                                                  | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-11 | F0-WRK-2 recon                        | `apps/workers/src/inboxSyncWorker.ts` uses `prisma.socialMessage.create()` directly (around line 161), bypassing `IngestSocialMessageUseCase` and the aggregate/outbox path. Result: messages ingested via sync (most of them) do NOT emit the `SocialMessageReceived` domain event, so any downstream event-driven handler (triage dispatch, notifications) does not fire for sync-ingested messages. The webhook path correctly goes through the use case. **Status uncertain**: intentional bypass (worker ergonomics, performance) vs canon drift vs forgotten refactor — needs classification. F0-WRK-2's `TriageDispatchEventHandler` covers webhook path; sync coverage is the gap.                                                                                                                                                                                                                                                              | Worker scaffolded before the canonical event-driven ingestion pattern stabilised.                                                              | PENDING | Post-fases sweep                                                                                                                                                                                                                                                     |
| SMELL-12 | F0-CLI-2 rewrite                      | Semantic inbox classification (`COMPLAINT` / `LEAD` / `QUESTION` / `FEEDBACK` / `SPAM`) used to live as a client-side enum (`InboxMessageType` in the old `useInbox/types.ts`) without any server source. F0-CLI-2 dropped it because the server does not compute it (server `messageType` is the wire type COMMENT / MENTION / REPLY / DIRECT_MESSAGE). If product wants a semantic classification, it must be a new output of the AI triage use case (server side, then exposed on `SocialMessageDTO`). **Status uncertain**: product feature gap vs deliberately abandoned scaffolding.                                                                                                                                                                                                                                                                                                                                                              | Inbox UX scaffolding ahead of an AI classification output the triage worker does not produce.                                                  | PENDING | Future triage-semantic-classification task                                                                                                                                                                                                                           |
| SMELL-13 | F0-CLI-2 rewrite                      | The server's `InboxFilter` (`SocialMessageQueryRepository.ts:44-53`) does NOT accept a `priority` filter, even though `priority` is the principal triage output and the canonical inbox-prioritisation UX wants a "show me URGENT only" pill. The new `InboxSidebar` therefore exposes type/status/platform filters only. To filter by priority server-side, the server's read-side query needs a new optional `priority?: InboxPriority` parameter wired through `GetInboxQuery` and `PrismaSocialMessageQueryRepository`.                                                                                                                                                                                                                                                                                                                                                                                                                             | Read-side filter surface predated the AI triage output.                                                                                        | PENDING | Future inbox-priority-filter task                                                                                                                                                                                                                                    |
| SMELL-14 | F0-CLI-2 rewrite                      | The inbox feed exposes only inbound messages — the server does not currently surface an `isOurReply` flag (or equivalent) that lets the client distinguish reply rows we sent from inbound ones. The new `MessageBubble` therefore renders every message as an inbound bubble (left-aligned grey) and drops the previous `direction: INBOUND \| OUTBOUND` axis. Once a server signal lands, the bubble can restore the outbound right-aligned styling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Visual axis carried over from an earlier shape that anticipated `direction` server-side.                                                       | PENDING | Future server-side outbound-reply-flag task                                                                                                                                                                                                                          |
| SMELL-15 | F0-CLI-2 rewrite                      | `apps/client/tests/integration/hooks.integration.test.ts` was a 100% placeholder: 5 `describe.todo` + 22 `it.todo`, zero assertions (commit `85b6b7e`). Audited one-by-one: all 5 referenced hooks are live + used (FORGOTTEN-FEATURE, not DEAD). `useAutoSave` + `useProviders` already had dedicated real tests (10 todos redundant); `useABTests` / `useTemplates` / `useTemplateVersions` (used by TemplateManagementDashboard) had NO coverage. Several todo descriptions didn't match the real hook APIs.                                                                                                                                                                                                                                                                                                                                                                                                                                         | TDD placeholders staged ahead of writing real assertions.                                                                                      | FIX     | 2026-05-22 — placeholder deleted; 3 dedicated MSW v2 integration suites written for the uncovered hooks (useTemplates/useABTests/useTemplateVersions, 15 tests). Client suite now 0 todo.                                                                            |
| SMELL-16 | F0-WRK-3 implementation               | Trend-radar multi-source supports per-source filtering via `TrendingDataPort.fetchTrends({ accountId, sources? })` and adapters honour the filter, but there is **no plan-tier / on-demand billing gate** wrapping the `MultiSourceTrendingDataAdapter`. Per Edward's directive (2026-05-20) Perplexity Sonar is intended as a feature plus / on-demand billing add-on; until the gate ships, every account that runs detection consumes Perplexity quota regardless of plan. F0-WRK-3 ships without the gate by design (architecture supports it; policy not yet implemented).                                                                                                                                                                                                                                                                                                                                                                         | Multi-source trend detection wired ahead of the plan-tier billing matrix.                                                                      | PENDING | Future billing-gating task (likely Fase 1)                                                                                                                                                                                                                           |
| SMELL-17 | F0-WRK-3 implementation               | `AIServicePort.generateStructured` does not expose Perplexity Sonar's native citation surface (citation position, URL, share-of-voice). `PerplexityTrendingAdapter` workaround: the structured-output schema includes `sourceUrl?: string \| null` and the prompt asks the model to embed citation URLs in the response. Functional but weaker than the native citation field — citations may be hallucinated, share-of-voice cannot be computed. Future canon refinement: extend `AIServicePort` with a citation-aware search method (or a dedicated `WebSearchPort`) that surfaces the provider's native citation structure.                                                                                                                                                                                                                                                                                                                          | Cross-provider AI substrate (`AIServicePort`) generalised before web-search citations existed.                                                 | PENDING | Future AI-citation-port task                                                                                                                                                                                                                                         |
| SMELL-19 | F0-CLI-3 manual E2E smoke test        | `apps/client/next.config.mjs` had a global rewrite `{ source: "/api/:path*", destination: "http://localhost:3000/:path*" }` that pre-empted the dedicated route handler `app/api/backend/[...path]/route.ts`. Next.js evaluates rewrites BEFORE the file-system router, so every `/api/backend/<path>` call was forwarded to `http://localhost:3000/backend/<path>` (with `backend/` prefixed) — the API has no `/backend/*` mount, so the response was `Route GET:/backend/<path> not found`. Effect: **every client→API call returned 404 in runtime** (repurpose, inbox, trends, billing, posts, campaigns, ALL hooks). Bug latent through F0-CLI-1 and F0-CLI-2 because unit tests used `vi.stubGlobal("fetch", ...)` and never exercised the proxy. Fixed inline in F0-CLI-3 (rewrite removed; route handler is the single canonical proxy, injects the customer Bearer from the httpOnly cookie). Verdict: FIX (close phase F0-CLI-3 2026-05-20). | Next.js v16 evaluation order quirk + missing manual smoke test in prior client tasks.                                                          | FIX     | F0-CLI-3 (2026-05-20) — global rewrite removed; route handler `[...path]` is the canonical proxy; smoke-test `curl http://omnipost-dev:3200/api/backend/trends/radar` returns 401 (was 404).                                                                         |
| SMELL-18 | F0-API-3 recon                        | The legacy trends module (`apps/api/src/trends/trendRoutes.ts` + `TrendAnalysisService.ts` + `TrendReportBuilder.ts` + `trendTypes.ts` + their tests) ships 5 GET endpoints (`/trends/{analysis,viral,opportunities,predictions,report}`) over a `TrendAnalysisService` that talks to a stub TikTok client + circuit breaker and **returns `[]` by default**. Zero callers in `apps/client/` or `apps/admin/`. The module is independent of the F0-WRK-3 `TrendRadarResult` pipeline — it does not consult the table that F0-WRK-3 populates and that F0-API-3's new `/trends/radar` endpoint exposes. F0-API-3 deliberately left the legacy surface intact: chain-deleting 5 endpoints + 1 service + 1 builder + 1 types file + their tests requires its own audit (regla "Enumerate chain-deletes explicitly"). **Status uncertain**: pre-B/F0 forgotten-feature vs scheduled future re-wire.                                                         | TikTok-trend scaffolding pre-B/F0; intended path never materialised — `TrendRadarResult` is the canonical model now.                           | PENDING | Post-fases sweep (chain-delete task)                                                                                                                                                                                                                                 |
| SMELL-20 | F1-API-1 implementation               | The `post_edit` hook (`.claude/hooks-py/post_edit.py`) ran `secretlint` without an explicit `cwd`, inheriting `apps/api` as the working directory. Combined with the `@secretlint/node` patch (`patches/@secretlint__node@12.3.1.patch`, which fixes `node_moduleDir = <cwd>/node_modules`), the rule preset `@secretlint/secretlint-rule-preset-recommend` (installed only in the root `node_modules`) resolved against `apps/api/node_modules` where it does not exist. Effect: secretlint aborted with "rule module not found" and the hook reported it as a leak — a false positive on **every** edit under `apps/api`. lint-staged + `secret:scan` were unaffected (they run from the repo root). Fixed by passing `cwd=PROJECT_DIR` (repo root) to the hook's `subprocess.run`, aligning it with the other invokers; the third-party patch was left intact.                                                                                       | Hook authored before the `node_moduleDir` patch made cwd load-bearing.                                                                         | FIX     | F1-API-1 (2026-05-21) — `post_edit.py` runs secretlint from the repo root.                                                                                                                                                                                           |
| SMELL-21 | F1-API-1 implementation               | RAG end-to-end is not yet validated against live providers: all AI provider keys (`OPENAI_API_KEY`, `GEMINI_API_KEY`, `PERPLEXITY_API_KEY`) are empty in `.env` (placeholder comments only), so embeddings cannot be computed and the content LLM cannot run. `GenerateLocalizedContentUseCase` degrades gracefully (brand-voice + locale-native, empty `usedTerms`/`usedRules`); glossary/style-guide upserts persist the row with `embeddingPersisted: false` and a NULL `embedding` column. The grounded-success path (200 + content + cosine retrieval over the HNSW index) is covered deterministically by the trajectory eval with mocked providers, but the live `<=>` cosine search + auto-embedding-on-upsert have not run against real vectors. **Action**: once an OpenAI/Gemini key is configured, re-run `tests/integration/aiLocalizedRoutes.test.ts` and add the grounded-200 assertions.                                                | Embeddings infra shipped ahead of provider-key provisioning.                                                                                   | PENDING | When an OpenAI/Gemini key is configured (validate RAG end-to-end).                                                                                                                                                                                                   |
| SMELL-22 | F1-CLI-1 implementation               | `apps/admin` uses next-intl in the **cookie-based "without i18n routing"** mode (`apps/admin/i18n/request.ts` resolves locale from the `NEXT_LOCALE` cookie + `Accept-Language`, no `[locale]` route segment, no `proxy.ts` middleware for locale). The canonical next-intl App Router pattern (and the one F1-CLI-1 implemented for `apps/client`) is the `[locale]` dynamic segment + middleware + `generateStaticParams`. Per Edward's directive (canon over consistency, 2026-05-21) the inferior pattern is NOT replicated — `apps/client` follows the canon and `apps/admin` is left as debt to migrate. No URL-localized routes, no static-per-locale generation, no hreflang in admin until migrated.                                                                                                                                                                                                                                           | Admin i18n shipped before the `[locale]`-routing canon was applied repo-wide.                                                                  | FIX     | 2026-05-22 — admin migrated to `[locale]` routing (routing/navigation/request/global type-safe, proxy merge createMiddleware+auth, app/ under `[locale]`, switcher via locale navigation, setLocaleAction removed, i18n:lint). Default unified to `en` in both apps. |
| SMELL-23 | F1-WRK-1 implementation               | The provider `ProviderCapabilities` shape is defined in **four** places that have drifted: the port's inline `capabilities` object (`packages/ports/src/ProviderAdapter.ts`), `packages/providers/shared/src/providerTypes.ts` (`ProviderCapabilities`), `apps/api/src/providers/providerAdapter.interface.ts` (`ProviderCapabilities` + `LegacyProviderAdapter.capabilities`), and `packages/shared/src/providers/providerConfig.ts` (`ProviderCapabilities` + per-provider `PROVIDER_CONFIGS` literals). F1-WRK-1 added the new `mentions` flag only to the port (the runtime contract the worker gates on) + each adapter's `*_CAPABILITIES` const; the other three definitions still lack `mentions`. **Status:** pre-existing duplication, surfaced (not introduced) by F1-WRK-1; consolidating to a single `ProviderCapabilities` type is an architectural task, not inline scope.                                                                | Capability concept duplicated across port / shared / api / config before a single source of truth was established.                             | PENDING | Post-fases sweep (unify ProviderCapabilities)                                                                                                                                                                                                                        |
| SMELL-24 | F1-WRK-1 implementation               | Bluesky mention search (`BlueskyAdapter.searchMentions`) uses the **connected channel's authenticated session** (App Password → `app.bsky.feed.searchPosts`), so brand listening on Bluesky requires a connected Bluesky channel for credential resolution — uniform with the inbox/analytics workers. The F1-WRK-1 plan mentioned the **public AppView** (no auth) as a fallback when no credentials exist; deferred to keep the adapter free of a direct `@atproto` public-agent path and to keep one credential model. **Effect:** a project with tracked terms but no connected Bluesky channel gets no Bluesky mentions. Future: add an unauthenticated public-AppView search path.                                                                                                                                                                                                                                                                | v1 chose the uniform per-channel credential model over a second (public, no-auth) Bluesky code path.                                           | PENDING | Future bluesky-public-appview-search task                                                                                                                                                                                                                            |
| SMELL-25 | F1-WRK-1 implementation               | Mention→tracked-term attribution (`matchTrackedTermId` in `apps/workers/src/mentionIngestWorker.ts`) is a case-insensitive **substring** match returning the **first** matching term. Simplifications: (a) a mention matching several terms is attributed to only one (`Mention.trackedTermId` is single-valued); (b) a hit that matched a handle/hashtag synonym not present verbatim in the body gets `trackedTermId = null`. SoV in F1-API-1b can re-classify from `source` (WEBHOOK ⇒ own-brand) + `trackedTerm.kind`, but per-mention multi-term attribution + synonym mapping are not modelled in v1.                                                                                                                                                                                                                                                                                                                                             | v1 kept the corpus single-term + substring to ship ingestion before the SoV read model (F1-API-1b).                                            | PENDING | F1-API-1b (revisit attribution when computing SoV)                                                                                                                                                                                                                   |
| SMELL-26 | F1-API-3 implementation               | `apps/workers/src/*` job processors talk to **Prisma directly** instead of resolving application use cases from a DI container (e.g. `mentionIngestWorker.ts`, and the other workers in `apps/workers`). F1-API-3 deliberately did NOT replicate this: its bulk-schedule worker runs **in-process in `apps/api`** and resolves `ProcessBulkScheduleRowUseCase` / `FailBulkScheduleRowUseCase` from `app.container` (DI as premise, zero direct Prisma, no `apps/workers`→`apps/api` dependency), mirroring the repurpose/triage/trend consumers. The existing `apps/workers` direct-Prisma access is pre-existing debt surfaced (not introduced) here — migrating those workers to DI/use-cases is an architectural task, not inline scope. Until then `apps/workers` bypasses the aggregate/UoW/outbox guarantees the use cases enforce.                                                                                                               | Workers shipped with direct Prisma access before a DI composition root for `apps/workers` existed.                                             | PENDING | Post-fases sweep (migrate `apps/workers` processors to DI + application use cases)                                                                                                                                                                                   |
| SMELL-27 | F2 Bloque B (analytics activation)    | **Admin-side analytics never reviewed/activated.** Bloque B scoped strictly to the **client** analytics engine (ROI / cross-platform / performance / realtime / conversions — `apps/api/src/analytics/analyticsRoutes.ts`, all `requireClientAuth`; client dashboard `apps/client/.../dashboard/analytics`). The **admin** platform-operator analytics (`apps/api/src/admin/analyticsRoutes.ts`, `requireAdminAuth` + `requirePermission(ANALYTICS_READ / AUDIT_READ)`; single page `apps/admin/app/[locale]/(dashboard)/analytics/page.tsx`) was deliberately out of B's scope and is **not** present in `FEATURE_TRACE_MATRIX_ES.md` nor any implementation roadmap. Revisit: confirm its endpoints are wired (not 501), the data is real (not mocked), and it surfaces the multi-tenant platform KPIs a platform operator needs (revenue/MRR, account growth, usage aggregates, audit).                                                              | B scoped to the client analytics (product + sales point); admin operative analytics is a separate platform-operator concern.                   | PENDING | Dedicated admin-analytics review task (post Bloque B)                                                                                                                                                                                                                |
| SMELL-28 | Homologation (false-green repo tests) | **`Account.subscription` column was dropped (A2 → AccountSubscription) but two production sites still reference it.** (a) `PrismaAccountQueryRepository.updateSubscription` does `prisma.account.update({ data })` where `SubscriptionUpdateData.subscription?: SubscriptionTierKind` — passing `subscription` throws `Unknown argument subscription` at runtime (maxProjects/isOnTrial/trialEndDate are real columns and work). Live caller: `ChangeAccountSubscriptionUseCase.ts:250`. (b) `findById`/`findByEmail` return `account as unknown as AccountDto`, and `AccountDto.subscription` is declared but never populated (always `undefined`). Surfaced while resurrecting the renamed AccountQueryRepository integration test, whose `updateSubscription` block was removed per Edward's call. Decide: drop `subscription` from `SubscriptionUpdateData` + `AccountDto` (+ the use-case path), or reintroduce a real source for the tier.        | The subscription tier moved to the `AccountSubscription` relation; the old `Account.subscription` scalar references were not fully cleaned up. | PENDING | Post-fases sweep (subscription-model cleanup)                                                                                                                                                                                                                        |
| SMELL-29 | Homologation (false-green repo tests) | **`PrismaAccountRepository` (command repo: findById/findByEmail/save/delete/hardDelete/exists/findAll) has no dedicated integration test.** The file historically named `AccountRepository.test.ts` actually exercised `PrismaAccountQueryRepository` (read side) and was renamed to `AccountQueryRepository.test.ts` during homologation; the command repo's `save`/`delete`/`hardDelete` paths are only covered indirectly by flow tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | The mis-named test file masked the absence of command-repo coverage.                                                                           | PENDING | Future AccountRepository (command) integration-test task                                                                                                                                                                                                             |

## Notes

- F0-API-1's own new code follows the fail-loud canon
  (`tests/integration/repurposeRoutes.test.ts`) and the typed
  `request.customerUser` accessor — it does **not** reproduce SMELL-1 or
  SMELL-2.
- This backlog is fed continuously; absence of an entry is not evidence
  of absence — the Bloque B revisit (post-Fase 0) is a scheduled pass.
- **AUDIT-1 pass (2026-05-19)** — dedicated read-only audit over the 72
  `.ts/.tsx` files touched by B1/B2/B3/B5 + F0-WRK-1/F0-API-1/F0-CLI-1.
  Verified with `git blame` per finding; fitness greps scoped to the
  perimeter were 0 across canon checks. By Edward's call SMELL-5..8
  include pre-existing tech debt **surfaced while editing the file**
  even though it was not introduced by B/F0 — the alternative (leaving
  the spotted debt only in the audit transcript) is a soft time bomb.
  Origin "AUDIT-1 (pre-existing, surfaced)" makes the provenance
  explicit so the post-fases sweep can route them to whichever
  workstream they actually belong to.
