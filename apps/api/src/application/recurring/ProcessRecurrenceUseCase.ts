/**
 * @file ProcessRecurrenceUseCase.ts
 * @description Re-export of the canonical definition, which lives in `@core/application/recurring/ProcessRecurrenceUseCase.js`.
 * @layer application
 */

export * from "@core/application/recurring/ProcessRecurrenceUseCase.js";
