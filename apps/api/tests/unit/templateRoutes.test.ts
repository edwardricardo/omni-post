// Tests moved to split files:
// - templateRoutes.crud.test.ts     (CRUD operations)
// - templateRoutes.actions.test.ts  (duplicate, compile, validate, track usage)
// - templateRoutes.features.test.ts (versions, A/B tests, platform limits)
// - templateRoutes.env-setup.ts     (DATABASE_URL env shim)
// - templateRoutes.test-helpers.ts  (shared mock service and app factory)
