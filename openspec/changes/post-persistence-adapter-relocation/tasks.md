# Tasks: Relocate the Post persistence adapters to `@adapters/db-prisma`

**Change** `post-persistence-adapter-relocation` · **Branch** `workstream/post-persistence-adapter-relocation` (base `main` @ `c23ec2c8`) · **Inputs** `specs/post-persistence-adapter-boundary/spec.md` (8 requirements / 25 scenarios, R5 amended to the three relocated subdirectories) and `design.md` rev 1.1 (D1–D13, gate PASS after C1–C4).

Every number below was RE-MEASURED against the working tree in this phase with `Grep`/`Read` (no Bash in this agent). Where a number could only be produced by RUNNING something (coverage, suite outcomes), it is marked **measure at apply** rather than estimated. Design C1–C4 were re-derived and all four hold; five NEW corrections (C5–C9) are recorded below because the apply's checklist is where a wrong count becomes a wrong edit.

---

## Review Workload Forecast — MEASURED

```text
Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: Medium
```

**Which number that budget line refers to, stated so nobody is misled.** `Medium` is the **CODE** tier — ≈ **209** changed lines against the 400 hard budget (52 %) — under the two-tier instrument in force for this repo (CODE 400 hard / EVIDENCE pre-approved with ONE decision per change, recorded in this forecast). The raw combined `additions + deletions` is ≈ **956** across ~83 files, of which **EVIDENCE ≈ 747** is mechanical constructor and specifier churn. Under a single-tier 400 instrument this reads `High`; the number is on the record either way, and the ONE decision the two-tier convention requires is the line below.

| Field                        | Value                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Estimated changed lines      | ≈ 956 total (CODE ≈ 209 · EVIDENCE ≈ 747), ~83 files                                                                                                                                                                                                                                                                                                                     |
| 400-line budget risk         | Medium on CODE (209/400); EVIDENCE over budget by design and pre-approved here                                                                                                                                                                                                                                                                                           |
| Chained PRs recommended      | **No** — orchestrator decision (D11). W4 must land in the SAME PR as W1 so `main` never holds the window in which the relocated files sit outside every gate (the "dead scope = infallible gate" class spec R5 opens by naming), and W1 cannot be split without a non-green commit: every re-point and construction must land with the move for `tsc` and vitest to pass |
| Suggested split              | Single PR, five work-unit commits W1 → W2 → W3 → W4 → W5                                                                                                                                                                                                                                                                                                                 |
| Delivery strategy            | single-pr (two-tier budget; `size:exception` per PR is retired — the tier split travels in the PR body)                                                                                                                                                                                                                                                                  |
| Chain strategy               | n/a for this change. The session's `feature-branch-chain` applies to N-COR-8, whose tracker `feature/post-publish-partial-failure` bases on THIS branch after this apply passes its gate                                                                                                                                                                                 |
| Decision needed before apply | **No** — the one two-tier decision is recorded in this table; R1 (coverage) is the only conditional escalation and is not askable until W1 has measured AFTER                                                                                                                                                                                                            |

### Work units — per-unit measured sizes

| WU     | Commit (Conventional)                                                                | Files | CODE  | EVIDENCE | Total     | Focused test command                                                                                 | Runtime harness                                                             | Rollback boundary                                                                                                       |
| ------ | ------------------------------------------------------------------------------------ | ----- | ----- | -------- | --------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **W1** | `refactor(persistence): relocate Post persistence adapters to @adapters/db-prisma`   | ~74   | ≈ 143 | ≈ 573    | **≈ 716** | `pnpm --filter @apps/api test` + `pnpm --filter @adapters/db-prisma test`                            | `pnpm db:up` then the six named integration batches (LXC-safe, single-file) | one revert restores every specifier, construction and the `TX_SEAMS` term; window closes when N-COR-8 PR 1b bases on it |
| **W2** | `refactor(persistence): translate relocated adapter comments to English`             | 2     | ≈ 66  | 0        | **≈ 66**  | `pnpm --filter @apps/api test`                                                                       | N/A — comments only, no runtime behaviour                                   | independently revertible; carries no behaviour                                                                          |
| **W3** | `test(persistence): keep the nesting-adjudication guard over the relocated adapters` | 1     | 0     | ≈ 35     | **≈ 35**  | `pnpm --filter @apps/api exec vitest run tests/unit/infrastructure/tenantTransactionNesting.test.ts` | N/A — a filesystem walk over the repo, no services                          | independently revertible; guard only                                                                                    |
| **W4** | `chore(fitness): extend the seam, raw-query, any and ts-ignore scopes to db-prisma`  | 2     | 0     | ≈ 115    | **≈ 115** | the five edited fitness blocks, each red then green                                                  | N/A — shell gates                                                           | independently revertible, but NOT before W1 is reverted (W1 depends on the #40 A term, which ships in W1)               |
| **W5** | `docs: re-path the relocated adapters in ADR-0023/0014/0005 and the guards doc`      | 5     | 0     | ≈ 24     | **≈ 24**  | `pnpm format:check`                                                                                  | N/A — docs                                                                  | independently revertible                                                                                                |

### How each line item was sized (opened, not extrapolated)

| Item                                              | Measurement                                                                                                                                                                                                                                                                                | Lines |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- |
| In-file edits, `PrismaUnitOfWork.ts`              | 175 ln file read: import `:11` extends, `:14` deleted, ctor `:75-78` +1 param +1 type import, `:103` derivation                                                                                                                                                                            | ≈ 10  |
| In-file edits, `PrismaPostRepository.ts`          | imports `:28`/`:35`, ctor `:54-57`, and the 3 seam sites. **Character-counted:** `:566` is 86 chars today; `getAmbientGucScope()` (20) → `resolveGucScope(this.tenantProvider)` (36) makes it **102** > `printWidth: 100`, so all three reflow 3 lines → 7 with a re-indented body         | ≈ 40  |
| Barrel `packages/adapters/db-prisma/src/index.ts` | 197 ln read; `PrismaMentionRepository` export at `:41`; 4 export lines + 6 comment lines + blank                                                                                                                                                                                           | ≈ 11  |
| 3 composition roots                               | `setupRepositories.ts:98-109`/`:217` read (3 import lines collapse to 1), 2 use-case roots +1 import +1 arg each                                                                                                                                                                           | ≈ 20  |
| 17 `apps/api/src` re-points                       | one import line each, measured file by file                                                                                                                                                                                                                                                | ≈ 36  |
| 26 test-file re-points                            | 19 integration (28 specifier lines collapsing) + 6 unit + 1 root. **`PrismaUnitOfWork.test.ts`'s 16 dynamic imports are TWO lines each** (C7) → 32 deletions for 1 added static import                                                                                                     | ≈ 94  |
| 64 constructions                                  | 54 `new PrismaUnitOfWork(` + 10 `new PrismaPostRepository(`, enumerated below; single-line sites reflow where they pass 100 chars                                                                                                                                                          | ≈ 145 |
| 27 provider-import lines                          | 12 new lines + 15 extensions of an existing `security/tenantContext.js` import                                                                                                                                                                                                             | ≈ 45  |
| 3 stale constructor-form comments (C5)            | `hardDeleteSerializableRace.test.ts:16`, `setupProjectUseCases.test.ts:143-144`, `setupAccountUseCases.test.ts:145-146`                                                                                                                                                                    | ≈ 10  |
| Nesting LOUD half                                 | 223 ln file read: `HELPER:42`, 3 `INLINE_ADJUDICATED:49-53`, 7 `INDEPENDENT_BY_DESIGN:60-73`, `relative():180`, `readFileSync` at `:189`/`:200`/`:210`, `apiSrc:36`, import `:32`, construction `:117`                                                                                     | ≈ 37  |
| New seam proofs                                   | `PrismaUnitOfWork.test.ts` +2 cases (the `createOutcomeRecordingPrismaClient` at `:55-80` already records `$queryRaw`) ≈ 55; `PrismaPostRepository.test.ts` +1 ≈ 33; `softDeleteJoinsUnitOfWork.test.ts` +1 (its `spyClient()` at `:29-39` gains `post: { count }`) ≈ 39                   | ≈ 127 |
| NEW `setupRepositories.test.ts`                   | sized against its comparable `setupProjectUseCases.test.ts` (read: 199 ln, header 15 + imports 12 + container double 16 + `tokensResolvedBy` 10 + one behavioural case 57)                                                                                                                 | ≈ 115 |
| #40 A hunk                                        | `CLAUDE.md:1424` + `.github/workflows/fitness.yml:1389`, one code line each + comment paragraph                                                                                                                                                                                            | ≈ 20  |
| W2 translation                                    | 27 comment lines in `PrismaUnitOfWork.ts` (`:19`, `:22`, `:24`, `:26`, `:31-33`, `:57`, `:59-61`, `:63-65`, `:68`, `:70-71`, `:81-84`, `:159-162`, `:167`, `:169`) + 5 single-line comments in `PrismaPostRepository.ts` (`:145`, `:557`, `:608`, `:649`, `:665` — all read, all one line) | ≈ 66  |
| W4 gate edits                                     | `CLAUDE.md` #3 `:339` · #5 `:379` · #23 `:550` · #40 B `:1454-1472`; `fitness.yml` #3 `:119`/`:123` · #5 `:187` · #23 `:534` · #40 B `:1415-1423`                                                                                                                                          | ≈ 115 |
| W5 docs                                           | 6 measured stale file-path lines + 3 symbol-level sentences + 1 dated blockquote                                                                                                                                                                                                           | ≈ 24  |

### Per-file size watch

- `PrismaPostRepository.ts` — **903 lines pre-existing**, unchanged by this change (≈ 40 lines touched). The CQRS split is backlog **B6**; do NOT start it here.
- Rename-detection headroom, computed: repository ≈ 40/903 → **96 % similar**; UoW ≈ 10/175 → **94 %**; `PostAggregateMapper.ts` and `PrismaOutboxWriter.ts` → **100 %** (their sibling-relative imports `./mappers/PostAggregateMapper.js` and `../unitofwork/PrismaUnitOfWork.js` are byte-identical at the new layout). All four clear `git diff -M`'s 90 % default.
- `CLAUDE.md` and `.github/workflows/fitness.yml` grow ≈ 58 lines each; both are token-gated (see T4.0).

---

## Tasks-time corrections (C5–C9) — measured in this phase

| #      | Design said                                                                                                        | Measured now                                                                                                                                                                                                                                                                                                                                                                      | Consequence                                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **C5** | C3: 3 comment-only stale references (`Container.ts:21`, `ChannelAuthFailureRecorder.ts:11`, `tenantContext.ts:10`) | `new PrismaUnitOfWork(` has **60** occurrences, 54 executable; the other **6** are comments in **4** files. Three of those files are NOT in C3: `hardDeleteSerializableRace.test.ts:16`, `setupProjectUseCases.test.ts:143-144`, `setupAccountUseCases.test.ts:145-146` — each names a constructor form this change makes wrong                                                   | +1 task (T1.17), +10 lines. Same "doc comment that lies" class spec R6 names for the `@example`                                                                    |
| **C6** | D13 R10 lists the 24 edited test files as the run-checklist                                                        | `setupProjectUseCases.test.ts` and `setupAccountUseCases.test.ts` construct NOTHING but drive the roots that do, through a container double whose `resolve` answers unknown tokens with `{}` (`:48`). They are affected suites and **must run**                                                                                                                                   | added to the W1 run list (T1.20). Their `{}` default is also why D1 refuses a `TOKENS.TenantContextProvider`: `resolveGucScope({})` throws on `getSystemContext()` |
| **C7** | D8: "16 dynamic `await import()`"                                                                                  | Each is **two** lines (`const { PrismaUnitOfWork } =` / `await import("…");`), so the collapse is 32 deletions for 1 addition. Confirmed **zero** `vi.mock` / `vi.doMock` / `vi.resetModules` in that file, so the collapse is behaviour-preserving                                                                                                                               | forecast +17 lines; T1.12 states the shape                                                                                                                         |
| **C8** | spec R8 scenario: search `docs/technical/` and `docs/security/` for "the four old paths"                           | A **directory-level** grep also hits `ADR-0011:128` (a still-true historical statement about a different workstream) and `MULTI_TENANT_AUDIT_2026-05-27.md:33,:73,:74,:93` (a dated audit record — the same class as `TENANT_RLS_AB_MEASUREMENT.md`). The deciding grep MUST be **file-level**: measured **6** matches today, all in `docs/technical/`, **0** in `docs/security/` | T5.4 pins the exact expression. Without it the apply chases a false red and rewrites a dated record                                                                |
| **C9** | W5: `MULTI_TENANT_GUARDS.md:1665` (scope sentence)                                                                 | The #23 scope claim is **two** sentences: `:1664-1665` ("greps `apps/api/src` and `apps/workers/src`") and `:1671-1672` ("Any NEW raw query under `apps/api/src` or `apps/workers/src` still needs an explicit exception and an ADR"). Both go stale when #23's scope grows                                                                                                       | T5.2 names both                                                                                                                                                    |

**Design C1–C4 re-verified, all four hold.** C1: 54 executable UoW sites in 24 files (3 production · 28 across 17 integration files · 1 root test · 22 across 3 unit files) and 10 repository sites — exact. C2: the #23 UoW exception is a whole-file filter, so the red path must plant in the relocated **repository**. C3: the 3 path-stale comments are the only path-stale ones. C4: the flat db-prisma files hold exactly **6** `withGucBoundTransaction(` sites (`MentionRepository.ts` ×2, `ProjectRepository.ts` ×2, `PostRepository.ts` ×2), so Part B's second scope is the three subdirectories, never the package root.

**Baselines re-measured at tasks time** (all match the spec's pinned values): `withGucBoundTransaction(` in `apps/api/src` = 15 call lines, minus `sagaTenant.ts` ×2 exempt = **13** = Part B `SITES`; after the move 10 + 3 = **13**, floor 10, margin 3. `.$transaction(` in Part A's scope = **3** (`PrismaUnitOfWork.ts:89`, `ChannelRepository.ts:72`, `:118`; the `:59` hit is a comment line the gate filters) = `SEAM_HITS` floor 3. `rg '@adapters/db-prisma/' apps packages` = **0**. Coverage literals at `apps/api/vitest.config.ts:119-122` = 56.8 / 57.3 / 47.8 / 56.2, `autoUpdate` at `:127`.

---

## W1 — the move, the injection, the wiring, the #40 A term, the coverage measurement

> One commit. RED tests first (strict TDD is active). The apply NEVER runs git: it creates each file at its new path with byte-identical content, deletes the old path, and applies the surgical edits in place; the orchestrator stages the pair and `git diff -M` detects the rename by similarity. Keep W1's content edits to the ones listed — every extra byte spends rename headroom (spec R6.S1).

- [x] **T1.1** RED — add the two injected-provider seam cases to `apps/api/tests/unit/infrastructure/PrismaUnitOfWork.test.ts` using D1's fixed doubles (`{ getTenantContext: () => ({ accountId: "acc-injected" }), getSystemContext: () => undefined }` and a system double), importing `PrismaUnitOfWork` from `@adapters/db-prisma`. Assert `set_config` carries `acc-injected`, and `__system__` under the system double, reusing `createOutcomeRecordingPrismaClient` (`:55-80`). **Expected red:** the specifier does not resolve / the constructor takes no provider — confirm the red is THAT, not a typo. (D1) — spec R2.S4, R3.S3
- [x] **T1.2** RED — add one case to `apps/api/tests/unit/infrastructure/PrismaPostRepository.test.ts`: a standalone `create` binds the INJECTED scope through `withGucBoundTransaction`; give the tx double at `:110-139` a `$executeRaw: vi.fn(async () => 1)` because `setTenantGuc` issues one. **Expected red:** constructor arity / unresolved specifier. (D1) — spec R2.S1
- [x] **T1.3** RED — add one cross-boundary case to `apps/api/tests/unit/infrastructure/repositories/softDeleteJoinsUnitOfWork.test.ts`: inside ONE `executeInTransaction`, an `apps/api` repository (`PrismaProjectRepository`) and the relocated `PrismaPostRepository` both resolve the active transaction client; assert `clients.tx.project.update` and `clients.tx.post.count` were called and `clients.base.post.count` was NOT. Add `post: { count: vi.fn(async () => 1) }` to `spyClient()` (`:29-39`). **Expected red:** the relocated repository is not importable yet. (D7 — this proves the static accessor across the package boundary; it is NOT a src/dist duality detector) — spec R4.S1
- [x] **T1.4** RED — create `apps/api/tests/unit/infrastructure/container/setupRepositories.test.ts` (no such file exists — measured), modelled on `setupProjectUseCases.test.ts:141-197` (read-only comparable): resolve the `TOKENS.UnitOfWork` and `TOKENS.PostRepository` factories from a container double whose `$transaction` spy yields `{ $queryRaw, $executeRaw, post: {…} }`; under `withTenantContext({ accountId: "acc-root" })` the UoW issues `set_config` and the repository's standalone `create` binds; with no context neither binds. **Expected red:** the root passes no provider. (D1) — spec R2.S3
- [x] **T1.5** Move the four files with content byte-identical: `apps/api/src/infrastructure/repositories/PrismaPostRepository.ts` → `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts`; `…/repositories/mappers/PostAggregateMapper.ts` → `…/src/post/mappers/PostAggregateMapper.ts`; `apps/api/src/infrastructure/outbox/PrismaOutboxWriter.ts` → `…/src/outbox/PrismaOutboxWriter.ts`; `apps/api/src/infrastructure/unitofwork/PrismaUnitOfWork.ts` → `…/src/unitofwork/PrismaUnitOfWork.ts`. Delete the four old paths. The mapper and the outbox writer change **zero bytes**. (D1, spec R1) — spec R1.S1, R6.S1
- [x] **T1.6** GREEN — `packages/adapters/db-prisma/src/unitofwork/PrismaUnitOfWork.ts`: extend the `tenantGuc.js` import with `resolveGucScope`, delete the `getAmbientGucScope` import, add `import type { TenantContextProvider } from "@infra/prisma/extensions/tenantGuard.js";`, make the constructor `(prisma, tenantProvider, options?)` with the provider REQUIRED, and derive `const scope = resolveGucScope(this.tenantProvider);`. Leave `txStorage` and `static getTransactionClient()` untouched. (D1, D7) — spec R2.S2, R3.S1, R4.S2
- [x] **T1.7** GREEN — `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts`: same import surgery; constructor `(prisma, outboxWriter: OutboxWriter | undefined, tenantProvider)` with the provider REQUIRED (TS1016 forbids an optional before a required one, so the outbox slot becomes explicitly `| undefined`); rewrite the three seam calls to `withGucBoundTransaction(this.prisma, resolveGucScope(this.tenantProvider), …)`. All three reflow to the multi-line form (measured: 101/102/102 chars > `printWidth: 100`), landing the token on call+2 — inside #40 Part B's call+3 window. (D1, D2, R12) — spec R2.S1, R3.S2
- [x] **T1.8** GREEN — append D8's block to `packages/adapters/db-prisma/src/index.ts` after `:41`: the four exports (`PrismaPostRepository`, `PostAggregateMapper` + `type PrismaPostWithRelations`, `PrismaOutboxWriter`, `PrismaUnitOfWork` + `type TransactionOptions`) and the comment distinguishing the `@ports/core` DTO factory `createPostRepository` (`:36`) from the `@core/domain` aggregate adapter. Word it so it names no `apps/api` path (the T1.20 boundary grep is deliberately over-strict and reads comments too). Zero new package dependencies — verified: `package.json:26-36` already declares `@core/domain`, `@infra/prisma`, `@shared/types`. (D8) — spec R1.S1
- [x] **T1.9** GREEN — wire the three composition roots, each `+ import { ambientTenantContextProvider } from "../../security/tenantContext.js";`: `apps/api/src/infrastructure/container/setupRepositories.ts` (`:104-107` repository, `:217` UoW; its three old import lines collapse into one `@adapters/db-prisma` import), `setupProjectUseCases.ts:71-74` and `setupAccountUseCases.ts:71-74` (provider in slot 2, `HARD_DELETE_TX_OPTIONS` stays THIRD). Pass the provider as an imported VALUE, never a container token. (D1) — spec R2.S3, R3.S1, R3.S3
- [x] **T1.10** Re-point the 17 `apps/api/src` importers to `@adapters/db-prisma` (root specifier only, never a subpath): the 3 roots above plus `infrastructure/unitofwork/tenantTransaction.ts:31` (**sibling-relative `./PrismaUnitOfWork.js`** — invisible to a path-fragment grep), `repositories/PrismaAuditLogRepository.ts`, `PrismaConversionRepository.ts`, `PrismaAccountRepository.ts`, `PrismaAdminSessionRepository.ts`, `PrismaBulkScheduleBatchRepository.ts`, `PrismaCrisisProjectRepository.ts`, `PrismaTrackedLinkRepository.ts`, `PrismaProjectRepository.ts`, `PrismaCustomerUserRepository.ts`, `PrismaAdminUserRepository.ts`, `PrismaAccountSubscriptionAdapter.ts`, `adapters/PrismaCustomerMfaUserRepository.ts`, `adapters/PrismaAdminMfaUserRepository.ts`. (D8) — spec R1.S1, R4.S2
- [x] **T1.11** Re-point the 19 integration files (multi-specifier files collapse to one import line) and `apps/api/tests/accountLifecycle.test.ts`: `helpers/publishNowPromotionHarness.ts`, `sagaCompensationRecovery`, `tenantGucTransactionBinding`, `recurringPostTenantIsolation`, `mfaBackupCodeSingleUse.integration`, `customerPasswordReset.integration`, `repositories/PrismaPostRepository.test`, `sagaCrashRecovery`, `externalNotificationTenantIsolation`, `generatedImageTenantIsolation`, `bulkScheduleMediaPath.integration`, `post-trio-tenant-isolation`, `channelTenantIsolation`, `bulkScheduleRelayRetry.integration`, `trackedLinkTenantIsolation`, `scheduledReportTenantIsolation`, `hardDeleteSerializableRace`, `bulkScheduleOutboxSmoke`, `campaignTenantIsolation`. (D8) — spec R1.S1
- [x] **T1.12** Re-point the 6 unit files: `outbox/PrismaOutboxWriter.test.ts`, `infrastructure/PrismaPostRepository.test.ts`, `infrastructure/tenantTransactionNesting.test.ts:32`, `infrastructure/unitofwork/tenantTransaction.test.ts`, `infrastructure/repositories/softDeleteJoinsUnitOfWork.test.ts`, and `infrastructure/PrismaUnitOfWork.test.ts` — where the **16 two-line dynamic imports** (C7, at `:90-91, :105-106, :116-117, :129-130, :143-144, :160-161, :181-182, :197-198, :210-211, :228-229, :247-248, :255-256, :270-271, :284-285, :322-323, :337-338`) collapse into ONE static `import { PrismaUnitOfWork } from "@adapters/db-prisma";`. Verified safe: that file has zero `vi.mock` / `vi.doMock` / `vi.resetModules`. (D8, C7) — spec R1.S1
- [x] **T1.13** Update all **64** constructions. **54 `new PrismaUnitOfWork(`**: production `setupRepositories.ts:217`, `setupProjectUseCases.ts:71`, `setupAccountUseCases.ts:71`; integration `bulkScheduleOutboxSmoke:84,:189,:251`, `publishNowPromotionHarness:208,:356`, `campaignTenantIsolation:142`, `tenantGucTransactionBinding:272,:358`, `recurringPostTenantIsolation:147,:408,:420`, `mfaBackupCodeSingleUse:349,:391,:410,:487`, `customerPasswordReset:218,:698`, `externalNotificationTenantIsolation:150`, `generatedImageTenantIsolation:160`, `bulkScheduleMediaPath:60`, `channelTenantIsolation:159`, `bulkScheduleRelayRetry:62`, `hardDeleteSerializableRace:212`, `sagaCrashRecovery:822`, `trackedLinkTenantIsolation:155`, `scheduledReportTenantIsolation:146`, `post-trio-tenant-isolation:529,:543`; root `accountLifecycle:52`; unit `PrismaUnitOfWork.test` ×16 (`:92,:107,:118,:131,:146,:162,:183,:199,:212,:230,:257,:272,:286,:287,:324,:339`), `tenantTransactionNesting:117`, `softDeleteJoinsUnitOfWork:65,:81,:107,:123,:150`. The **three option-bearing** sites (`setupProjectUseCases:71`, `setupAccountUseCases:71`, `hardDeleteSerializableRace:212`, plus `mfaBackupCodeSingleUse:410` and `PrismaUnitOfWork.test:146`) keep their options in the THIRD position. **10 `new PrismaPostRepository(`**: `setupRepositories.ts:104`; one-arg → `(X, undefined, provider)` at `recurringPostTenantIsolation:138`, `sagaCompensationRecovery:412`, `tenantGucTransactionBinding:175`, `integration/repositories/PrismaPostRepository.test:49`, `sagaCrashRecovery:805`, `post-trio-tenant-isolation:439`, `unit/infrastructure/PrismaPostRepository.test:150`; two-arg → `publishNowPromotionHarness:199,:354`. (D1, C1) — spec R3.S3
- [x] **T1.14** Add the provider import to the 27 files that need it — 12 new import lines (`mfaBackupCodeSingleUse`, `accountLifecycle`, `bulkScheduleMediaPath`, `bulkScheduleRelayRetry`, `bulkScheduleOutboxSmoke`, `tenantTransactionNesting`, `softDeleteJoinsUnitOfWork`, both `PrismaPostRepository.test` files, the 3 roots) and 15 extensions of an existing `security/tenantContext.js` import. Every tier injects `ambientTenantContextProvider` — one rule, no per-file judgement; the NEW seam proofs (T1.1–T1.3) are the only fixed doubles. (D1) — spec R2.S3
- [x] **T1.15** `apps/api/tests/unit/infrastructure/tenantTransactionNesting.test.ts` — LOUD half (D6 W1): introduce `const repoRoot = join(currentDir, "..", "..", "..", "..", "..");` (`:31` already climbs five levels to reach `infra/`), keep `apiSrc` derived from it, make every key repo-root-relative (`HELPER:42`, the 7 `INDEPENDENT_BY_DESIGN` keys `:60-73`, and `INLINE_ADJUDICATED:49-53` whose first entry becomes `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts`), and change `readFileSync(join(apiSrc, relPath))` at `:189`, `:200`, `:210` to `join(repoRoot, relPath)` and `relative(apiSrc, path)` at `:180` to `relative(repoRoot, path)`. The walk stays single-root in W1 (population 15 ≥ floor 8, still green) — nothing is retired between W1 and W3. (D6) — spec R5.S5
- [x] **T1.16** Fix the 3 path-stale comments (C3): `apps/api/src/infrastructure/container/Container.ts:21` `@example` → `new PrismaPostRepository(prisma, outboxWriter, ambientTenantContextProvider)`; `apps/workers/src/services/ChannelAuthFailureRecorder.ts:11` "in apps/api" → "`PrismaOutboxWriter` in `@adapters/db-prisma`, `OutboxRelay` in apps/api"; `apps/api/src/security/tenantContext.ts:10` → the new UoW path. Leave `setupPostUseCases.ts:206`, `tests/unit/helpers/mockPrisma.ts:704` and `IntegrationEventDeliveryHandler.ts:73` alone — symbol-level and still true, and `mockPrisma.ts:704` additionally carries a section marker that the pre-edit tripwire blocks on any edit of that line. (D8, C3)
- [x] **T1.17** Fix the 3 stale constructor-form comments found at tasks time (C5): `apps/api/tests/integration/hardDeleteSerializableRace.test.ts:16` and the twin comments at `apps/api/tests/unit/infrastructure/container/setupProjectUseCases.test.ts:143-144` and `setupAccountUseCases.test.ts:145-146`. Update only the constructor EXPRESSIONS they quote; the claim they make (dropping the options argument leaves the resolved token set identical) stays true and its wording stays. (C5) — spec R6 class
- [x] **T1.18** Re-path fitness #40 Part A's seam term in BOTH files, byte-identical in the code line: `CLAUDE.md:1424` and `.github/workflows/fitness.yml:1389` → `TX_SEAMS='/db-prisma/src/unitofwork/PrismaUnitOfWork\.ts:|/saga/sagaTenant\.ts:|/db-prisma/src/ChannelRepository\.ts:'`, plus D3's comment paragraph naming the new home AND the honest statement that the `/saga/sagaTenant\.ts:` term is INERT (measured: zero `.$transaction(` in that file; the floor of 3 is carried by two files). Do NOT delete the inert term — deletion is an authorisation (backlog B1). This rides in W1 because without it W1 fails twice on its own. (D3) — spec R5.S1, R5.S6
- [x] **T1.19** Demonstrate #40 Part A's red: revert the `TX_SEAMS` term alone (old path back) → `SEAM_HITS=2` → scope error, real non-zero exit. Restore byte-exact (checksum) → `SEAM_HITS=3`, `COUNT=0`. Capture the transcript for the PR body. Never edit the gate to manufacture its own red. (D3) — spec R5.S1, R5.S6
- [x] **T1.20** GREEN gate for W1: `pnpm lint --max-warnings 0` · `NODE_OPTIONS=--max-old-space-size=6144 pnpm exec tsc -b apps/api packages/adapters/db-prisma` · `pnpm --filter @adapters/db-prisma typecheck` · `pnpm --filter @apps/api test` · `pnpm --filter @adapters/db-prisma test` · `pnpm --filter @apps/api test:all` (DB + Redis via `pnpm db:up`) · `madge --circular apps/api/src/ packages/` · `pnpm build`. **Run these suites explicitly** because NO tsc project type-checks them (R10): the 24 construction-site files, plus `setupProjectUseCases.test.ts` and `setupAccountUseCases.test.ts` (C6). Static invariants: `rg '@adapters/db-prisma/' apps packages` → **0**; `rg getAmbientGucScope packages/` → **0**; `rg -n "apps/api|security/tenantContext" packages/adapters/db-prisma/src` → **0**; fitness #38 both streams re-measured (**0** swept-tree / **11** db-prisma) and pasted, not inferred. — spec R1.S2, R1.S3, R2.S1, R2.S2, R2.S3, R2.S4, R3.S1, R4.S1, R5.S7
- [x] **T1.21** Coverage AFTER (D10, R1 is HIGH): `cp apps/api/vitest.config.ts "$SCRATCH/vitest.config.ts.before"` → `pnpm --filter @apps/api test:unit:coverage` → read the four AFTER values from `apps/api/coverage/coverage-summary.json` (read-only) → `cp` back and `cmp` to prove a byte-exact restore, because `autoUpdate` at `:127` rewrites the literals upward on any run that measures above them → re-run `tests/unit/config/coverageThresholds.test.ts` to confirm the restored file still parses → record BEFORE (59.58 / 59.95 / 49.88 / 59.01) and AFTER in the PR body. **If ANY AFTER value < its literal (56.8 / 57.3 / 47.8 / 56.2): STOP and escalate with both numbers.** Never lower a literal, never add a `canon-exception` marker (no scenario in the allowed list describes a relocation), never neutralise the ratchet. Do NOT commit the `autoUpdate` raise here — backlog B12. (D10) — spec R7.S1, R7.S2
- [x] **T1.22** Stop at the W1 boundary and report to the orchestrator for the commit (the apply never runs git). Report: the four moved paths, the 64 construction sites touched, the #40 A transcript, the coverage BEFORE/AFTER pair, and the #38 numbers.

## W2 — English comments, strictly after the move

- [ ] **T2.1** Translate the 27 Spanish comment lines in `packages/adapters/db-prisma/src/unitofwork/PrismaUnitOfWork.ts` (`:19`, `:22`, `:24`, `:26`, `:31-33`, `:57`, `:59-61`, `:63-65`, `:70-71`, `:81-84`, `:159-162`, `:167`, `:169`) and update the `@example` at `:68` to the three-parameter constructor `new PrismaUnitOfWork(prisma, tenantProvider)`. Translate meaning, not word order; keep the measured hazards the comments state (the `runWithBoundGuc` marker paragraph at `:108-113` is already English and stays). (D11 W2) — spec R6.S2
- [ ] **T2.2** Translate the 5 single-line Spanish comments in `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts` (`:145`, `:557`, `:608`, `:649`, `:665` — each one line, all read). Leave the three `DELIBERATE soft-delete-sweep exception` markers byte-exact: fitness #38 greps that literal. (D11 W2) — spec R6.S2
- [ ] **T2.3** W2 gate: `rg -n "[áéíóúñÁÉÍÓÚÑ¿¡]"` over the four relocated files → **0**, and — because that grep is a FLOOR, not the proof (an accent-free Spanish sentence passes it) — confirm by reading that every block the explore enumerated is now English. Then `pnpm lint --max-warnings 0` · `tsc -b` · `pnpm --filter @apps/api test` · fitness #8 / #9 / #10. Introduce no sprint, phase or work-unit reference in any comment (tripwire #6). — spec R6.S1, R6.S2

## W3 — the nesting walk (the SILENT half)

- [ ] **T3.1** RED — in `apps/api/tests/unit/infrastructure/tenantTransactionNesting.test.ts`, add `SEAM_FILE_POPULATION = 16` with `expect(openers.length).toBeGreaterThanOrEqual(SEAM_FILE_POPULATION)` replacing the `>= 8` floor at `:184`, and the `toContain` loop over `[HELPER, ...INLINE_ADJUDICATED, ...Object.keys(INDEPENDENT_BY_DESIGN)]`. Against W1's single-root walk this is RED twice: population 15 < 16, and the relocated repository is named but not walked. **Precondition verified at tasks time:** all 11 named files are real seam openers (1 helper + 3 inline + 7 independent), and 5 further adopters are unnamed and covered by the count only. (D6) — spec R5.S5
- [ ] **T3.2** GREEN — add `SEAM_ROOTS = ["apps/api/src", "packages/adapters/db-prisma/src/post", "…/outbox", "…/unitofwork"]` with D6's JSDoc (the flat db-prisma files keep the explicit-scope worker convention and stay outside on purpose) and make the walk `SEAM_ROOTS.flatMap((root) => walk(join(repoRoot, root)))` with `relative(repoRoot, path)`. Population returns to **16**. (D6) — spec R5.S5
- [ ] **T3.3** Demonstrate D6's three reds, each a real failing run followed by a byte-exact restore: (i) drop the `…/src/post` root → count red AND `toContain` red; (ii) misspell a root → `readdirSync` ENOENT (loud); (iii) drop the root and lower `SEAM_FILE_POPULATION` to 15 → the `toContain` loop is still red (the counting mask stays closed). Transcribe for the PR body. (D6) — spec R5.S5

## W4 — the remaining gate scopes (same PR as W1)

- [ ] **T4.0** Before editing: `CLAUDE.md` and `.github/workflows/fitness.yml` are token-gated sensitive paths, and the #23 block additionally contains a pre-existing section marker that the pre-edit tripwire blocks on any edit of that line. A `sensitive-edit` token is ACTIVE tonight and gates BOTH surfaces; if it has expired, STOP and report — do not paraphrase the gate, do not split the pair, do not work around the hook. Every edit below is byte-identical between the two files in the **grep expressions and code lines**; shell plumbing (`MATCHES=$(…)`, `::error title=…`, `if/exit`) and comment language differ by design.
- [ ] **T4.1** #40 Part B — apply D2's block to `CLAUDE.md:1454-1472` and `.github/workflows/fitness.yml:1415-1423`: add `PART_B_SCOPE` (the three relocated subdirectories beside `apps/api/src`) with its own fail-closed existence loop, point `seam_call_sites()` at `$PART_B_SCOPE`, admit the second expression in the window test (`getAmbientGucScope\(\)|resolveGucScope\(this\.tenantProvider\)`), keep `SCOPE_EXEMPT` at ONE file, keep the floor at **10** (measured population 13 → margin 3), and rewrite residual (2) and residual (4) as D2 words them. (D2) — spec R5.S2, R5.S6
- [ ] **T4.2** #40 Part B reds: (i) plant `this.tenantProvider.getTenantContext()?.accountId` at the relocated `create` site → `BCOUNT=1`, exit 1; (ii) the same plant with `PART_B_SCOPE="apps/api/src"` → `BCOUNT=0`, proving the extension is load-bearing; (iii) misspell one directory → scope error, exit 1. Byte-exact restore after each (checksum), then re-confirm `SITES=13`, `BCOUNT=0`. (D2) — spec R5.S2, R5.S6
- [ ] **T4.3** #23 — extend the scope to `packages/adapters/db-prisma/src` in `CLAUDE.md:550` and `.github/workflows/fitness.yml:534`, update the "Scope:" description sentence, re-path the UoW exception filter to `/db-prisma/src/unitofwork/PrismaUnitOfWork\.ts`, and replace its bullet with D4's TWO bullets: the exception is **INERT** (the statement is a tagged template and the regex is anchored on `(`, so it has never matched) and `packages/adapters/db-prisma/src` entered the scope with **0** paren-form calls, its one tagged-template site `resilience.ts:308` getting NO exception line. (D4) — spec R5.S3, R5.S6
- [ ] **T4.4** #23 red (C2-corrected): plant `await tx.$queryRawUnsafe("SELECT 1");` as a real statement inside `doCreate` in `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts` — NOT in the UoW, whose whole file is excepted under both scopes. Old scope → **0** (the dead scope proving itself), new scope → **1**, exit 1. Byte-exact restore → **0**. (D4, C2) — spec R5.S3
- [ ] **T4.5** #3 — name `packages/adapters/db-prisma` in the scope at `CLAUDE.md:339` and `.github/workflows/fitness.yml:119` (existence loop) and `:123` (grep scope), with D5's one-sentence comment (measured 0 at entry). (D5) — spec R5.S4
- [ ] **T4.6** #5 — add `packages/adapters/db-prisma/src/` explicitly at `CLAUDE.md:379` and `.github/workflows/fitness.yml:187`, with D5's comment stating WHY (`packages/*/src/` is one level deep and never reached a two-level package) and that the general widening is unmeasured (backlog B8). (D5) — spec R5.S4
- [ ] **T4.7** #3 / #5 reds: plant `const leak = {} as any;` as a statement in the relocated repository → #3 reports 1, exit 1; plant a `@ts-ignore` line above a statement in the same file → #5 reports 1, exit 1. Byte-exact restore after each → both **0**. (D5) — spec R5.S4, R5.S6
- [ ] **T4.8** Run all 41 fitness checks locally to 0 and diff the edited blocks of `CLAUDE.md` against `.github/workflows/fitness.yml` to confirm the grep expressions and code lines are byte-identical. Paste every red transcript and every final count in the PR body. — spec R5.S6, R5.S7

## W5 — docs

- [ ] **T5.1** Re-path the 6 measured stale file-path citations: `docs/technical/ADR-0023-unit-of-work-result-aware-transaction.md:33`, `:90`, `:238`, `:240`; `docs/technical/ADR-0014-multi-tenant-isolation-guards.md:120` (stale TODAY — names `apps/api/src/unitofwork/`, a directory that does not exist); `docs/technical/ADR-0005-unit-of-work-asynclocalstorage.md:195` (stale TODAY — names `apps/api/src/infrastructure/persistence/`). (D11 W5) — spec R8.S1
- [ ] **T5.2** `docs/security/MULTI_TENANT_GUARDS.md` — extend BOTH #23 scope sentences (C9): `:1664-1665` ("greps `apps/api/src` and `apps/workers/src`") and `:1671-1672` ("Any NEW raw query under `apps/api/src` or `apps/workers/src`…"), and name the new UoW location in the exception mentions at `:1669` and `:1682`. These are symbol-level references — measured: this file carries NO full old path. (C9, D11 W5) — spec R8.S1
- [ ] **T5.3** `docs/reports/TENANT_RLS_AB_MEASUREMENT.md` — add ONE dated blockquote under the title naming the relocation. Do NOT touch a single existing `Source site` citation (8 of them name the relocated repository): it is a dated measurement record, and rewriting its citations would falsify what was measured and when. `scripts/rls-ab-measurement.ts:770-853` `sourceSite` literals are equally historical and stay (backlog B10). (D11 W5) — spec R8.S2
- [ ] **T5.4** W5 gate — run the FILE-level deciding grep (C8), never the directory-level one: `rg -n "apps/api/src/infrastructure/(repositories/(mappers/)?(PrismaPostRepository|PostAggregateMapper)|outbox/PrismaOutboxWriter|unitofwork/PrismaUnitOfWork)\.ts|apps/api/src/(unitofwork|infrastructure/persistence)/PrismaUnitOfWork\.ts" docs/technical docs/security` → **0** (6 today). A directory-level grep would falsely flag `ADR-0011:128` and the dated `MULTI_TENANT_AUDIT_2026-05-27.md`. Then `pnpm format:check`. — spec R8.S1, R8.S2

---

## Requirement → Task matrix (8 requirements · 25 scenarios · all MERGE-BLOCKING)

| Req | Scenario                                                        | Kind        | Tasks                     | Deciding command / expected                                                                                                                                                                                                                               |
| --- | --------------------------------------------------------------- | ----------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | the four resolve from the package root                          | static      | T1.5, T1.8, T1.10–T1.12   | each of the four named in the barrel; every consumer imports the bare `@adapters/db-prisma`                                                                                                                                                               |
| R1  | zero subpath imports exist                                      | static      | T1.20                     | `rg '@adapters/db-prisma/' apps packages` → **0** (0 today; must stay 0)                                                                                                                                                                                  |
| R1  | both consumers typecheck and build                              | static      | T1.20                     | `tsc -b apps/api packages/adapters/db-prisma` and `pnpm build` → exit 0, no warning                                                                                                                                                                       |
| R2  | no relocated file imports `apps/api`                            | static      | T1.6, T1.7, T1.8, T1.20   | `rg -n "apps/api\|security/tenantContext" packages/adapters/db-prisma/src` → **0**                                                                                                                                                                        |
| R2  | the ambient accessor is gone from packages                      | static      | T1.6, T1.7, T1.20         | `rg getAmbientGucScope packages/` → **0**                                                                                                                                                                                                                 |
| R2  | GUC binding and tenant isolation unchanged                      | integration | T1.9, T1.14, T1.20        | `integration:tenant-isolation` batch green; diff of `tenantGucTransactionBinding` + `post-trio-tenant-isolation` shows specifier/constructor edits only, **no assertion change** (one comment clause at `tenantGucTransactionBinding:274-277` is allowed) |
| R2  | a system-context call still binds `__system__`                  | integration | T1.1 (unit double), T1.20 | the system-context path in `integration:tenant-isolation`; `resolveGucScope` returns `SYSTEM_TENANT_SCOPE` when `getSystemContext()` is set (verified: `tenantGuc.ts:119-124`)                                                                            |
| R3  | Serializable hard-delete path still gets its options            | integration | T1.9, T1.13, T1.20        | `integration:hard-delete-race` batch green with the options in slot 3                                                                                                                                                                                     |
| R3  | omitting the tenant provider does not compile                   | static      | T1.6, T1.7, T1.20         | `tsc -b` rejects a two-argument `new PrismaPostRepository(...)`; the provider is REQUIRED on both classes                                                                                                                                                 |
| R3  | every construction site was updated deliberately                | static      | T1.13                     | 54 UoW + 10 repository sites, enumerated in T1.13; the 5 option-bearing sites keep position 3                                                                                                                                                             |
| R4  | relocated and non-relocated share one transaction client        | unit        | T1.3                      | the cross-boundary case in `softDeleteJoinsUnitOfWork.test.ts`: `clients.tx.post.count` called, `clients.base.post.count` not                                                                                                                             |
| R4  | the static accessor survives for its existing callers           | static      | T1.6, T1.10               | `getTransactionClient()` stays STATIC; the 16 `apps/api` callers keep the static form through the root specifier                                                                                                                                          |
| R5  | #40 Part A holds at the new seam path                           | static      | T1.18, T1.19              | `SEAM_HITS=3`, `COUNT=0` (baseline re-measured: 3 seam calls in scope today)                                                                                                                                                                              |
| R5  | #40 Part B covers the relocated sites with margin               | static      | T4.1, T4.2                | `SITES=13`, floor 10 (margin 3), `BCOUNT=0`, one admitted derived token                                                                                                                                                                                   |
| R5  | #23 can now see a raw query in the relocated code               | static      | T4.3, T4.4                | plant in the relocated REPOSITORY: old scope **0**, new scope **1**, restored **0**                                                                                                                                                                       |
| R5  | #3 and #5 cover the new directory                               | static      | T4.5–T4.7                 | each reports non-zero with its plant, **0** after a checksum-verified restore                                                                                                                                                                             |
| R5  | the nesting adjudication reaches the relocated repository       | unit        | T1.15, T3.1–T3.3          | walk includes `packages/adapters/db-prisma/src`; floor = measured population **16**; ENOENT and silent shrink both closed                                                                                                                                 |
| R5  | the guard pair stays byte-identical, each edit has a proven red | static      | T1.18, T1.19, T4.1–T4.8   | expressions and code lines identical in both files; five red transcripts (plant → real non-zero exit → checksum restore → expected count)                                                                                                                 |
| R5  | the unedited guards are re-measured, not assumed                | static      | T1.20                     | fitness #38 → swept-tree **0**, db-prisma ratchet **11**, both pasted                                                                                                                                                                                     |
| R6  | the four files are renames, not rewrites                        | static      | T1.5–T1.7, W2 ordering    | `git diff -M --stat main` (orchestrator) — computed headroom 96 % / 94 % / 100 % / 100 %                                                                                                                                                                  |
| R6  | no Spanish comment survives in the relocated files              | static      | T2.1–T2.3                 | the accent grep → **0**, plus the reviewer's block-by-block confirmation (the grep is a floor)                                                                                                                                                            |
| R7  | #37 measures zero with the literals untouched                   | static      | T1.21                     | fitness #37 → **0**; the four literals at `vitest.config.ts:119-122` unchanged                                                                                                                                                                            |
| R7  | before/after coverage recorded; a descent escalates             | static      | T1.21                     | BEFORE 59.58 / 59.95 / 49.88 / 59.01 and AFTER both in the PR body; on any breach STOP, never a marker                                                                                                                                                    |
| R8  | no doc still names an old adapter path                          | static      | T5.1, T5.2, T5.4          | the FILE-level grep of C8 over `docs/technical docs/security` → **0** (6 today)                                                                                                                                                                           |
| R8  | the dated measurement record keeps its citations                | static      | T5.3                      | `git diff main -- docs/reports/TENANT_RLS_AB_MEASUREMENT.md` is empty or exactly one dated note                                                                                                                                                           |

---

## Verification and gates (D13, with the LXC-measured commands)

| Gate             | Command                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Expected                                                       |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| Lint             | `pnpm lint --max-warnings 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 0 error / 0 warning                                            |
| Format           | `pnpm format:check`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | clean                                                          |
| Types            | `NODE_OPTIONS=--max-old-space-size=6144 pnpm exec tsc -b apps/api packages/adapters/db-prisma` and `pnpm --filter @adapters/db-prisma typecheck` — **`turbo run typecheck` OOMs in this LXC; use tsc directly**                                                                                                                                                                                                                                                                                                                                                                                                               | 0                                                              |
| Unit             | `pnpm --filter @apps/api test` · `pnpm --filter @adapters/db-prisma test`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | green                                                          |
| Integration      | `pnpm db:up`, then `pnpm --filter @apps/api test:all`. `apps/api/scripts/run-tests.sh` (read-only) loads the environment itself — **never print a secret, never hand-build a connection string** (the homelab Postgres rejects the credential the docs quote). Batches that carry the seven behaviour proofs: `integration:repositories` (`:221`), `integration:hard-delete-race` (`:245`), `integration:outbox` (`:254`), `integration:tenant-isolation` (`:274`), `integration:mfa-backup-single-use` (`:313`), `integration:saga-recovery` (`:336`). On LXC run them as heap-capped single files under a `timeout` wrapper | green, **no assertion edits**                                  |
| Fitness          | all 41 locally (the `CLAUDE.md` commands), five with transcribed reds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 41/41; #40 A 3/0 · #40 B 13/0 · #23 0 · #3 0 · #5 0 · #38 0/11 |
| Structure        | `madge --circular apps/api/src/ packages/` · `pnpm build`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 0 cycles; build green                                          |
| Boundary (R2)    | `rg -n "apps/api\|security/tenantContext" packages/adapters/db-prisma/src`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **0** — deliberately over-strict; comments included            |
| Comments (R6)    | `rg -n "[áéíóúñÁÉÍÓÚÑ¿¡]"` over the four relocated files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **0**, plus reviewer confirmation                              |
| Coverage         | T1.21                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | AFTER ≥ every literal, or STOP with both numbers               |
| Rename           | `git diff -M --stat main` (orchestrator)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | four renames                                                   |
| Worker invariant | `git diff main -- apps/workers/src/container/workerContainer.ts` (orchestrator)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **empty**                                                      |

---

## Rollback per work unit

- **W1** — one revert restores every specifier, every construction and the `TX_SEAMS` term together; the four files return to `apps/api/src`. The revert window closes when N-COR-8 PR 1b bases on these paths; state that in the PR body.
- **W2** — independently revertible; comments only, zero behaviour.
- **W3** — independently revertible; the guard returns to W1's single-root walk (still green, still LOUD-half correct).
- **W4** — independently revertible, but never before W1: W1 depends on the #40 A term it ships, and reverting W4 alone re-opens the gate window on the relocated files.
- **W5** — independently revertible; docs only.
- No schema, no data, no env, no flag — nothing to migrate back.

---

## Risks carried

| #       | Risk                                                                                                                      | Sev      | Mitigation in these tasks                                                                                                                                                                                                                        |
| ------- | ------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1      | #37 coverage descent with no admissible scenario                                                                          | **HIGH** | T1.21: measure AFTER, restore the config byte-exact, STOP on any breach. Never a marker, never a lowered literal, never a neutralised ratchet                                                                                                    |
| R10     | The 24 construction-site test files are type-checked by NO tsc project; a missed provider argument is a runtime TypeError | MED      | T1.20 runs every affected suite, plus the two root-driving suites C6 adds. Pre-existing gap → backlog B13                                                                                                                                        |
| R11     | W1 is ≈ 716 lines and atomic inside a single PR of ≈ 956                                                                  | MED      | Two-tier budget: CODE ≈ 209 inside the 400 hard limit; EVIDENCE ≈ 747 pre-approved by the ONE decision this forecast records; five work-unit commits keep review focus; W1 reviews as four renames plus mechanical re-points under `git diff -M` |
| R2      | #40 Part B at its floor                                                                                                   | MED      | T4.1: population back to 13 over floor 10, margin 3                                                                                                                                                                                              |
| R3      | Silent guard loss                                                                                                         | MED      | T1.18, T3.1–T3.3, T4.1–T4.7; honest comments on both inert lines; depcruise → backlog B5                                                                                                                                                         |
| R4      | Under-counted churn (dynamic imports, sibling-relative)                                                                   | MED      | T1.10 and T1.12 name both shapes explicitly; verification is `tsc -b` + full suites, never grep alone                                                                                                                                            |
| R5      | Options reinterpreted as a provider                                                                                       | MED      | T1.6/T1.13 keep options THIRD; `integration:hard-delete-race` is the existing proof                                                                                                                                                              |
| R6      | Rename detection lost                                                                                                     | MED      | W1 surgical (headroom computed per file), W2 strictly after                                                                                                                                                                                      |
| R7      | `apps/api` behaviour drift at the seam                                                                                    | MED      | The root injects the SAME object the retired accessor read (verified: `tenantContext.ts:159` vs `:177-179`); seven proofs run with no assertion edits                                                                                            |
| R8      | Barrel evaluation cost                                                                                                    | LOW      | One `AsyncLocalStorage` allocation, no I/O; state it in the PR body                                                                                                                                                                              |
| R9      | `dist/` staleness                                                                                                         | LOW      | Root specifier only + turbo `dependsOn: ["^build"]`; `pnpm build` green                                                                                                                                                                          |
| R12     | Prettier reflow moves the Part B token to call+2                                                                          | LOW      | Character-counted at tasks time (101/102/102 vs `printWidth: 100`); inside the call+3 window; T4.2(i) confirms green before the plant                                                                                                            |
| **R13** | The #23 block edit touches a line carrying a pre-existing section marker that the pre-edit tripwire blocks                | LOW      | T4.0: the active `sensitive-edit` token gates tripwire bypass as well as the sensitive paths; if it has expired, STOP and report rather than paraphrase                                                                                          |

---

## Handoff to N-COR-8 (`post-publish-partial-failure`)

The tracker branch `feature/post-publish-partial-failure` bases on **this** branch once this apply passes its gate. What PR 1b consumes, and the D12 amendments its design must absorb (recorded here because `openspec/changes/post-publish-partial-failure/design.md` (read-only) belongs to that change and is not edited by this one):

1. `PrismaUnitOfWork` is `(prisma, tenantProvider, options?)` — provider REQUIRED, options LAST. D2's `new PrismaUnitOfWork(workerGuardedPrisma, workerTenantProvider)` stays valid.
2. `PrismaPostRepository` is `(prisma, outboxWriter: OutboxWriter | undefined, tenantProvider)` — provider REQUIRED, the outbox slot positional and always written (`undefined` when none).
3. `workerTenantProvider` must satisfy the FULL `TenantContextProvider` — `getTenantContext()` **and** `getSystemContext()`; "no system context" is `getSystemContext: () => undefined`, not an absent method (`resolveGucScope` calls it first).
4. #23's `/unitofwork/PrismaUnitOfWork\.ts` exception was **inert** (tagged template), so S4's "goes stale" premise is corrected, not merely re-pathed; it now reads `/db-prisma/src/unitofwork/PrismaUnitOfWork\.ts` and says so.
5. Fitness impact in D1: #13, #14, #16 have no impact (measured); the live edits are #40 A/B, #23, #3, #5 and the nesting test. The "58-file import churn" is 43 external files + 4 moved.
6. #40 Part B's second admissible expression is `resolveGucScope(this.tenantProvider)`, and `packages/adapters/db-prisma/src/post` is INSIDE Part B: any `withGucBoundTransaction(` PR 1b adds there must derive its scope that way, and any `.$transaction(` it adds anywhere in scope fails Part A.
7. `tenantTransactionNesting.test.ts` lists `packages/adapters/db-prisma/src/post/PrismaPostRepository.ts` as `INLINE_ADJUDICATED` **and** names it in the `toContain` loop: PR 1b's `savePublication` must keep a `PrismaUnitOfWork.getTransactionClient()` probe in that file, or the guard goes red.
8. The unit suites for the four stay in `apps/api/tests/unit/` (backlog B7); PR 1b's mapper/repository tests go there too until B7 lands.
9. Import path for all four: the ROOT specifier `@adapters/db-prisma`. A subpath specifier resolves onto `default: ./dist/*` and creates a SECOND `AsyncLocalStorage` — silently writing outside the caller's transaction and outside its GUC binding.

---

## Decisions needed from Edward

**None.** Every fork is technical and decided with measured evidence. Two items are authorisation-only, NOT blocking and NOT product decisions, carried as backlog: **B1** (deleting #40 A's inert `/saga/sagaTenant\.ts:` term) and **B2** (deleting or closing #23's inert UoW exception with SMELL-111). The single conditional escalation is **R1**, and it is not askable until T1.21 has measured AFTER.

## Backlog rows (report, do not fix here)

Carried from design: **B1**–**B14**. Added at tasks time:

| Row     | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Class                    |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| **B15** | `apps/api/tests/unit/infrastructure/PrismaUnitOfWork.test.ts` carries Spanish `it(...)` titles (`"ejecuta el callback dentro de una transacción Prisma"`, `"devuelve el valor retornado por el callback"`, `"propaga errores lanzados dentro del callback"`, and more). It is NOT one of the four relocated files, so spec R6 does not reach it and translating it here would inflate a diff that must read as a rename. Named because W1 edits its neighbours | canon (comment language) |
| **B16** | `docs/security/MULTI_TENANT_AUDIT_2026-05-27.md` and `docs/technical/ADR-0011:128` name the old adapter directory in still-true historical statements. They are dated/historical records in the `TENANT_RLS_AB_MEASUREMENT.md` class and are deliberately NOT rewritten (C8)                                                                                                                                                                                   | doc note                 |
