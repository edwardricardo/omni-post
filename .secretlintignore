# Files where credential-shaped strings are intentional (test fixtures, docs,
# CI samples). Mirror the gitleaks allowlist philosophy: we trust these paths
# to contain only example/test values, never real secrets.
#
# Generated artefacts
node_modules/
**/node_modules/
.next/
**/.next/
dist/
**/dist/
.stryker-tmp/
**/.stryker-tmp/
**/coverage/
**/storybook-static/
pnpm-lock.yaml
pnpm-lock.yaml.baseline

# Env templates and test fixtures
.env
.env.example
.env.*.example
.env.test
.env.local
.env.*.local
apps/*/.env
apps/*/.env.test
apps/*/.env.example
apps/*/.env.*.example
apps/*/.env.local

# Test files (fixtures contain `postgres:password123@localhost` etc.)
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
apps/api/tests/**
apps/workers/tests/**
packages/*/tests/**
**/vitest.config.ts

# Stryker mutation reports — JSON dumps of test code, contains test fixtures
apps/api/reports/**

# Documentation (shows example connection strings)
docs/**
CLAUDE.md
.claude/**

# CI workflow files (use placeholder credentials in service definitions)
.github/workflows/**

# IDE configs (may have local debug connection strings)
.vscode/**

# Markdown reports / runbooks may include sample DSNs
**/*.md
